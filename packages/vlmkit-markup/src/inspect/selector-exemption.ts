/**
 * The `<selector>;<reason>` exemption form. Moved to `@mizchi/vlmkit-judge/allow.ts`
 * with the judges that consume it (it is pure: no page, no file system), and
 * re-exported here so every existing import keeps working.
 */
export * from "@mizchi/vlmkit-judge/allow.ts";
