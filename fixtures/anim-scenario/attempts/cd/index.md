# Batched Pandora's Box — scene order

1. `scene-1-run.json` (matrix) — the one concrete numeric run: box costs,
   revealed rewards, running cost and running best-so-far, ending in the
   final score 8.2.
2. `scene-2-branch.json` (state-machine) — the decision rule at the branch
   point: what happens on each of the two things box 2 could have shown,
   played as two paths from the same state, plus the paper's closing bound.
