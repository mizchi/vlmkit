{
  "_note": "The fact sheet for the checkout-calls brief, in the schema `.claude/skills/d2-diagram/assets/d2-facts.mjs` reads. v1 withheld it; from v2 it is the writer's. `order` is checked from the connections' y positions, so the seven messages must be drawn top to bottom in this order. What the sheet cannot see is which messages are returns and which is async — the render drops every connection style, so that distinction has to be in the labels, and it is checked by eye.",
  "boxes": ["browser", "gateway", "orders", "inventory", "broker"],
  "order": [
    "browser->gateway",
    "gateway->orders",
    "orders->inventory",
    "inventory->orders",
    "orders->broker",
    "orders->gateway",
    "gateway->browser"
  ],
  "deps": [
    "browser->gateway",
    "gateway->orders",
    "orders->inventory",
    "inventory->orders",
    "orders->broker",
    "orders->gateway",
    "gateway->browser"
  ],
  "exhaustive": true,
  "maxColumns": 80,
  "_byEye": {
    "returns": ["inventory->orders (reserved)", "orders->gateway (201 Created)", "gateway->browser (order id)"],
    "async": ["orders->broker (order.placed)"],
    "readableAsPasted": "no label broken across a line, no arrow whose ends you cannot tell apart"
  }
}
