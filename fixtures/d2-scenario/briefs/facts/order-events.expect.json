{
  "_note": "The fact sheet for the order-events brief, in the schema `.claude/skills/d2-diagram/assets/d2-facts.mjs` reads. v1 withheld it and scored the attempts against it afterwards (writer d shipped four phantom boxes); from v2 it is handed to the writer and `d2-facts --expect` exiting 0 is part of the done condition. `browser` is deliberately absent from `containers`: the brief names three regions but never says which holds the browser, so either placement passes.",
  "boxes": [
    "browser",
    "cdn",
    "gateway",
    "orders",
    "inventory",
    "billing",
    "shipping",
    "broker",
    "postgres",
    "redis",
    "stripe"
  ],
  "containers": {
    "edge": ["cdn", "gateway"],
    "cluster": ["orders", "inventory", "billing", "shipping", "broker", "postgres", "redis"],
    "outside": ["stripe"]
  },
  "deps": [
    "browser->cdn",
    "cdn->gateway",
    "gateway->orders",
    "orders->inventory",
    "orders->broker",
    "broker->billing",
    "broker->shipping",
    "orders->postgres",
    "shipping->postgres",
    "inventory->redis",
    "billing->stripe"
  ],
  "forbidden": [
    "orders->billing",
    "orders->shipping",
    "billing->orders",
    "gateway->inventory",
    "gateway->billing"
  ],
  "exhaustive": true,
  "maxColumns": 100
}
