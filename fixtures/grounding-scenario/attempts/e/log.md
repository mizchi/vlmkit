# Agent e — log

| task | coordinate | confidence | what told me |
|---|---|---|---|
| renew | (472, 91) | high | `t6 button "Renew plan" @ (472,91) 61x19 #renew-now [occluded-target]` and the issue line: `#renew-now (button "Renew plan") is covered at its own centre (447,96): a click there goes to #promo. This map aims at (472,91) instead, which does reach it, with 4px of room before the nearest thing that is not this element`. Verified with `--at 447,96` -> `#promo (not a target)` and `--at 472,91` -> `#renew-now (t6)`. The screenshot alone shows only a yellow "SAVE 20% ANNUAL" ribbon with a ~9px unlabelled blue sliver at its right edge; I would not have known a "Renew plan" button was there at all, let alone that its own centre is dead. |
| manage-globex | (458, 203) | high | `t8 button "Manage" @ (458,203) 38x15 [ambiguous-target]`, row order given by the ambiguous-target issue text: `(t7 at 458,177, t8 at 458,203, t9 at 458,230) ... (Acme Freight Team 18 Manage / Globex Media Enterprise 240 Man… / Initech Labs Starter 4 Manage)`. Confirmed with `--at 458,203` -> the tr:nth-of-type(2) button selector (2nd row = Globex, matching the screenshot's row order top-to-bottom). |
| export-csv | (60, 293) | high | `t11 button "Export invoices as CSV" @ (60,293) 7x7 [imprecise-target,crowded-target]`. Confirmed with `--at 60,293` -> `button.export (t11)`, and boundary-tested `--at 56,293`->export, `--at 63,293`->trash (t12), `--at 50,293`->refresh (t10). The screenshot shows this as one indistinguishable 21px-wide tri-colour blob; I could not have told the three icons apart or picked the middle one's centre without the map. |
| publish | (218, 357) | high | `t15 button "Send" @ (218,357) 34x18 #publish [imprecise-target,label-mismatch]` and `#publish reads "Send" on screen but its accessible name is "Publish changes"`. The screenshot only shows a ~6px sliver of a teal button at the very bottom edge — no legible text at all. The accessible name "Publish changes" is what actually matches the task wording ("Publish my change to the billing contact"), not the on-screen "Send". Confirmed `--at 218,357` -> `#publish (t15)`. |
| audit-log | (225, 14) | high | `t4 link "Audit log" @ (225,14) 29x14`, no risks listed (aimMargin 24). Matches my own pixel read of the nav bar text run at x≈211–239, y≈13–15. |
| account-menu | (621, 14) | high | `t5 button "RM" @ (621,14) 15x15 [label-mismatch]` — `button.avatar reads "RM" on screen but its accessible name is "Account menu"`. The visible label "RM" and task wording "my account menu" both point at the same avatar circle top-right; the accessible name confirms it. Confirmed `--at 621,14` and `--at 615,15` both -> `button.avatar (t5)` (generous aimMargin 160.1, no nearby competitor).
