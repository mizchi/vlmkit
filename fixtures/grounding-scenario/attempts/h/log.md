# Attempt h — log

| # | action | what I expected | what the next screenshot showed | how I knew |
|---|---|---|---|---|
| 1 | `wheel 93,120 240` | ticket list scrolls down, revealing tickets below "Seat count mismatch" (which was clipped at the panel's bottom border in shot-00, implying overflow) | shot-01: list now shows "Cannot remove teammate", "Dark mode contrast", "API key rotation docs", "Webhook signature 401" — all new titles, none matching the target | list content fully replaced with new titles; no "Payment webhook retries" yet, so I knew the list had scrolled but not far enough |
| 2 | `wheel 93,120 240` | list scrolls further, closer to the alphabetical/whatever ordering that would surface "Payment webhook retries" | shot-02: top item is now **"Payment webhook retries" — Wayne Enterprises · 14h**, followed by "Slow dashboard on mobile", "CSV encoding garbled", and a partially visible Cyrillic-titled ticket | zoomed crop of the list (5x upscale) showed the exact string "Payment webhook retries" with subtitle "Wayne Enterprises · 14h", an exact match to the brief's target title — not the similarly-themed decoy "Webhook signature 401" seen in shot-01 |
| 3 | `click 70,68` (on the "Payment webhook retries" row) | right-hand pane opens showing that ticket's detail with Reply/Archive/Delete buttons | shot-03: right pane header reads "Payment webhook retries" / "Wayne Enterprises · 14h", body text "Retries stop after the third attempt even though the endpoint returns 500. We expect five.", and the same row is now highlighted (light blue background) in the left list | the detail pane's title+subtitle exactly matched the target ticket's title and account name seen in shot-02, and the left-list row became highlighted — both independent confirmations I had the right ticket open, not a neighbor |
| 4 | `click 248,112` (the "Archive" button, middle of the three, white/outline, between blue "Reply" and red-outlined "Delete") | ticket becomes archived; list row shows some changed/struck-through state; no deletion | shot-04: the list row title "Payment webhook retries" now renders with a strikethrough, and a new small teal line of text, **"Archived."**, appeared directly under the three buttons in the detail pane | both the strikethrough on the exact row and the literal word "Archived." appearing where no status text existed before (compare shot-03, which had no such line) are unambiguous, purpose-built confirmation text/styling for the archived state |

Final state per session.json (harness-written) reflects these 4 actions: 2 wheel scrolls, 1 click to open "Payment webhook retries", 1 click on "Archive".
