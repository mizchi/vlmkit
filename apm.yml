name: vlmkit
version: 0.9.0
# Publish only the lightweight automatic router and its bundled workflows.
includes:
  - .apm/skills/vlmkit/
targets:
  - claude
dependencies:
  apm: []
  mcp: []
scripts:
  verify: "ls -1 .claude/skills | sort"
