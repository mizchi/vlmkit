# Sprintboard — judgment log

How this interactive app — kanban board with drag and drop was judged, in order. Rendered from `judgment.sqlite` by `examples/sites/judge.mjs`; the published page, with the screens the log keeps and every gate output, is <https://mizchi.github.io/vlmkit/sites/kanban/judgment/>.

**5** rounds · **89** gate runs (15 failing) · **68** screens in 59 shots (19 kept) · **59** looks · defects: **13** by eye, **8** by a gate · 12 gate false positive(s)

Brief: [`../briefs/kanban.md`](../briefs/kanban.md)

## Defects

| | found by | what | fix | checked by |
|---|---|---|---|---|
| D1 | eye (L5 on S5) | `.team .avatar` Team avatars overlap by 6px (8px on phones) so the initials collide with the next circle: at 1x the header reads as one run 'AKMRPNJWLO', and on the phone (L3) the first four are cut to 'AI', 'MI', 'PN', 'JV'. | Team avatars no longer overlap: side by side with a 4px gap and no canvas ring (styles.css .team / .avatar); phones keep 28px circles with the same gap. | L7 |
| D2 | eye (L1 on S1) | `.card-meta .avatar` Cards whose due chip is wide ('Due today', 'Overdue') wrap the assignee avatar onto a second meta line by itself (Navigation keyboard support, Search API pagination at 1280; again at 768 and 375 in L2/L6), so those cards are taller and ragged next to their neighbours. | Moved the assignee avatar out of the meta row into a new .card-top row beside the labels (margin-left:auto, left of the ⋯ corner button, which moved to top:8px to centre on that row). The meta row now holds only priority · checklist · due. 'Assigned to …' stays as sr-only text in the described meta (app.js renderCard). | L7 |
| D3 | eye (L4 on S4) | `.prio svg .bar` The priority icon draws every bar at 25% opacity whatever the level: High shows three pale peach bars, the same as Low and Medium, so the icon carries no information (the rule meant to fill the bars loses on specificity to '.prio svg .bar'). | Priority bars: unlit bars use a neutral --bar-off grey, lit bars currentColor; the lit-bar selectors now carry the same specificity as the base rule (.prio--high svg .bar) and come after it, so Low/Medium/High show 1/2/3 coloured bars. | L7 |
| D4 | eye (L3 on S3) | `.appbar .search @375` On phones 'New task' drops to its own third row under a full-width search field instead of sharing the search row — the field's intrinsic width plus the button exceed the 343px row. | Phone search field is flex: 1 1 0 with min-width 0, so it takes whatever the New task button leaves on the second app-bar row instead of pushing the button to a third row. | L8 |
| D5 | eye (L3 on S3) | `header.app-header @375` Phone header is ~480px of chrome: brand, search, New task, title, avatars on their own row, two rows of chips, a 'Showing all 14 cards' line and the switcher — the first card starts at y≈490 of 812, so the board is below the fold's midpoint. | Phone header compacted: New task shares the search row (D4), board-head/filter paddings 16/12px, and the unchanging 'Showing all 14 cards' line is visually hidden on phones while no filter is active (.filter-summary.is-idle, still in the accessibility tree as the live status; it reappears when a filter or search is applied). | L8 |
| D6 | gate (G1) | `.card-meta > .meta` Meta-row items are three different box heights (plain text ~17px, due chip 22px, avatar 24px), which G1 measures as 2–3.5px top-edge near-misalignment on 8 cards and which makes a wrapped second line sit unevenly. | Every .meta item is a 22px-high inline-flex box (chips and plain items alike), so the row shares one top edge; the 24px avatar left the row in D2. | G21 |
| D7 | gate (G3) | `.board-head, .filter-bar` Two spacing values sit just off the page's own scale: board-head padding-top 22px next to the 24px used 9x, filter-bar padding-bottom 18px next to 16px. | Snapped board-head padding-top 22→24px and filter-bar padding-bottom 18→16px onto the page's scale. | G22 |
| D8 | gate (G13) | `.card-item.is-dragging .card` While a card is dragged its source copy is faded to opacity .45 and becomes illegible — G13 measures 15.7:1 at rest dropping to ~3.0:1 mid-drag on SB-101..104. The source position should be shown as a placeholder, not as washed-out text. | The dragged card's slot is now a placeholder — accent-soft fill with a 2px dashed indigo outline and its contents visibility:hidden — instead of the card at opacity .45. The browser's drag image (taken before the class is added, one frame later) is what travels with the pointer. / Second pass after G23: the blank dashed slot still measured 15.6:1 → 1.8:1 'at opacity 1' — a slot with no text at all has nothing legible to measure. The placeholder now keeps the card's title visible in --ink-2 (≈6.9:1 on the accent-soft fill) while labels, meta, avatar and ⋯ are hidden, so the slot says what is leaving and stays readable. | G28 |
| D9 | gate (G13) | `.card (dragend)` A drag cancelled with Escape does not leave the card as it was: G13 finds 15–27% of the card's box still different after dragend (SB-101..104). Likely the 180ms opacity transition on .card still easing back when the drag ends. | Removed opacity from .card's transition list (it now transitions transform and box-shadow only, the settle), so removing .is-dragging in dragend restores the card in the same frame whether the drag dropped or was cancelled. | G26 |
| D10 | gate (G14) | `.card[role=button]` Cards open a modal dialog on Enter/click but do not say so (no aria-haspopup=dialog), so a screen reader hears a plain button — and G14 logs only 'Enter: focus moves within' for every card, never probing the dialog's focus trap and return that the brief requires. | Cards carry aria-haspopup="dialog" (app.js buildItem): a screen reader announces that Enter opens a dialog, and check interactions can recognise the card as a popup trigger and probe the trap and focus return. | G27 |
| D11 | gate (G16) | `.card settle / check animation` The brief's drop settle (≤200ms, removed by reduced motion and ?animate=0) is unmeasured: check animation found 'animations: 0' because the settle only runs after a drop and the page is still on load, and the settle was a transition trick that leaves nothing in document.getAnimations() once it ends — so no gate can say it has a visible effect, how long it takes, or that reduced motion removes it. | The settle is now a 180ms @keyframes animation (sb-settle, transform from translateY(-6px) rotate(-1.5deg) scale(1.03) to none, fill both) restarted on every pointer drop, menu move and added card; the keyboard lift moved to the individual rotate/scale properties so a finished settle cannot pin it. Reduced motion and .no-motion set animation:none. New documented parameter ?replay=drop runs one real drop on load (Hero copy → Review, position 2 of 3) through place → afterMove → settle, so check animation has something to sample; without it nothing moves on load. | G29 |
| D12 | eye (L9 on S9) | `#kb-hint` The keyboard hint bar is squeezed to ~640px on a 1280px screen (a fixed box at left:50% shrink-fits to the half of the viewport right of its left edge), so its status and key list each break onto two lines. | #kb-hint gets width: max-content (still capped at 100vw − 32px), so on desktop the status and the four key groups sit on one line; the phone layout keeps its full-width two-row bar (width:auto there). | L11 |
| D13 | eye (L10 on S10) | `.card-item (scrollIntoView while lifted)` While moving by keyboard, the carried card is scrolled only until it touches the viewport edge: at the bottom of a column its tilted corner is cut off by the screen, and in the middle columns it would land under the fixed hint bar. | Card items carry scroll-margin 12px 12px 112px, which scrollIntoView honours: a card carried to the bottom of a column is revealed with 112px below it — clear of the screen edge and of the ~62px hint bar at bottom:20px. | L11 |
| D14 | eye (L15 on S15) | `.wip-alert` The over-limit alert is a single three-line red paragraph at one weight: the status ('over by 1 card') and the advice read the same, and the block feels heavy under the header. | The alert is now two lines of different weight: 'Over the WIP limit by 1 card' in bold red leads, and 'Finish something before starting more.' follows on its own line in --ink-2 at weight 500 (app.js updateColumns, styles.css .wip-alert strong / .wip-advice). / Second pass after L16: .wip-advice gets text-wrap: pretty so the two-line advice never ends on a lone word. | L17 |
| D15 | eye (L22 on S22) | `.column-note` Column notes always blame 'the filter' ('No cards match the filter', '2 cards hidden by the filter') even when only a search is active and every chip is off, so the note points the reader at the wrong control. | updateColumns names the cause: 'the search' when only a query is set, 'the filter' when only a chip is on, 'the filter and search' for both — in both the empty box ('No cards match the search') and the hidden count ('2 cards hidden by the search'). | L24 |
| D16 | eye (L28 on S28) | `#kb-hint @375` On phones the hint bar's status orphans the position count ('…Review, position 1 of' / '3') and the whole bar runs four lines, covering the card below the carried one. | Hint status joins 'position N of M' with no-break spaces and uses text-wrap: balance; on phones the bar drops to 12.5px with tighter key gaps (app.js showHint, styles.css .kb-hint-status and the ≤640px .kb-hint rules). / Second pass after L29: while moving, the phone hint lays its four key groups on a 2×2 grid (← → column \| ↑ ↓ position / Space drop \| Esc cancel); the idle three-group hint stays a wrapping row. | L30 |
| D17 | eye (L33 on S33) | `#board scroll → closeMenu` On phones a tap on ⋯ in a column that is not snapped into place opens the menu and closes it in the same moment: the tap scrolls the board to bring the button into view (or a snap is still settling after a swipe), and every board scroll event closes the menu. S34 shows the menu working once the board is at rest. | Board scroll no longer closes the menu unconditionally: positionMenu() (split out of openMenu) re-anchors it under its ⋯ on every board scroll, and it closes only when the button has left the board's visible width (triggerInBoard). | L35 |
| D18 | eye (L31 on S31) | `.dlg-facts @375` On phones the dialog's facts panel wraps its values raggedly (name under the avatar, 'Fri, Sep 25, / 2026', Priority alone on a second grid row) instead of reading as a clean list. | At ≤640px the dialog facts are a one-column list: each fact a flex row with the small-caps label at the left and the value right-aligned. / Second pass after L36: the fact labels are flex: none + nowrap, so 'DUE DATE' stays one line and the value takes the remaining width. | L37 |
| D19 | eye (L38 on S38) | `.add-form (reveal)` Opening a column's add form (New task, or a header +) scrolls it only until it touches the viewport's bottom edge, so the form's panel sits flush with the screen edge and reads as clipped. | The add form carries scroll-margin 16px 12px 32px, so the reveal after New task / + / Add a card leaves 32px of column below it instead of stopping at the edge. | L40 |
| D20 | gate (G58) | `.column-head .count` Under 1.4x text inflation (G58's after-image) the In progress header wraps its title to two lines and breaks the WIP pill inside itself — 'WIPXX 3X /X' over '3X' — so the count spills out of its pill. A longer translated column name ('In Bearbeitung') in a 250px column would do the same: the pill has no nowrap and the header gives the title no room to wrap on its own. | Count pills and the + button are flex: none with white-space: nowrap, so 'WIP 3 / 3' never breaks; the h2 title gets min-width: 0 and overflow-wrap: break-word, so a long column name wraps inside the title instead of squeezing the pill. / Second pass after G59's after-image: with break-word the unbreakable 'progressXXXX' kept its min-content width inside the flex h2 and ran under the pill; overflow-wrap: anywhere lets the title's text shrink with the h2 (words still wrap at spaces first, and only a word longer than the whole line is broken). | G60 |
| D21 | gate (G61) | `.avatar` Avatars are fixed circles (24px on cards, 32px in the header), so any initials longer than two letters spill out: G61 measures 'MRX' at 26px inside the 24px circle on SB-105, SB-109 and SB-114, pushing each card's top row 2px past its box. Three-letter initials are ordinary (e.g. M. R. Díaz). | Avatars are min-width circles with inline padding and border-radius 999px (header 32px, phone 28px, cards and dialog 24px): two initials still render as a circle, longer ones widen into a pill. | G62 |

## Rounds

### R1 · first draft (builder)

- **N1** decision: Gate set, fixed up front. Base set from PROTOCOL + markup-assist done condition: check integrity, check copy --manifest copy.txt, check design, check composition, check color, check a11y contrast (light only: the brief defines no dark theme or theme parameter), check a11y focus, check a11y touch, scan scroll, scan handlers, check interactions. Added for this brief's behaviour (dynamic-markup routing): scan handlers --probe-drag (HTML5 DnD must really be wired: dragover preventDefault, dragstart filling the DataTransfer); check interactions --max-elements 60 (the board has ~50 controls and the default cap of 30 would stop before the Review/Done cards); check motion + check animation (the 180ms drop settle and its prefers-reduced-motion / ?animate=0 removal); check scroll and scan scroll at 375x812 (the snap row only exists on phones, so the default 1280 viewport would measure nothing); check breakpoints --sweep (three layouts: 4-up grid >=1180, sideways board 641-1179, snapped phone row <=640 — overflow must stay inside the board at every width); check grounding (can a pixel-driven agent tell 14 identical-looking card menus and 4 'Add a card' buttons apart).
- **G1** `vlmkit check integrity index.html` → exit 0 — verdict: NO DEFECTS, 8 WARN (0 fail, 8 warn, 300 exempted) ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G1))
  - ✗ **D6** defect `.card-meta > .meta`: Meta-row items are three different box heights (plain text ~17px, due chip 22px, avatar 24px), which G1 measures as 2–3.5px top-edge near-misalignment on 8 cards and which makes a wrapped second line sit unevenly.
- **S1** index.html · 1280×800 · full page, 2 screen(s) ([screens](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#S1))
  - 👁 **L1**: Desktop 1280, two screens (page is 932px tall). Reads in the right order: white app bar with the indigo logo mark + "Sprintboard", search field and the indigo "New task" button at the right; then "Website relaunch" as the largest text, caption "Sprint 14 · Sep 14 – 25 · 14 cards", filter chips (All filled dark, Design/Frontend/Backend/Bug with colour dots) and "Showing all 14 cards" flush right. Four equal columns on a light grey canvas, each a tinted panel with a border — boundaries are clear. Headers: status dot, name, count pill ("5", "WIP 3 / 3", "2", "4"), + at the right. Cards are white with soft shadows, label chips legible (purple Design, blue Frontend, green Backend, red Bug), bold 14px titles, meta row of priority / checklist / due / avatar. Problems I can see: (1) the team avatars top-right overlap so tightly that the initials run together — "AK MR PN JW LO" reads as one string and the letters touch the next circle; (2) two cards grow an extra row just for the avatar: "Navigation keyboard support" (Medium · 5/5 · Due today, then MR alone on a second line) and "Search API pagination" (Urgent · 4/5 · Overdue, JW alone below) — the wide due chips push the avatar off the row, so those cards are taller and ragged next to their neighbours; (3) the priority bar icons are washed out — Low, Medium and High all look like the same three pale orange bars at this size, so the icon adds nothing to the word. Short columns (Review, Done) stretch to the Backlog's height; the empty panel below "Add a card" reads as room to drop, not as a gap. No clipping, no overlap between cards, no orphaned words.
    - ✗ **D2** defect `.card-meta .avatar`: Cards whose due chip is wide ('Due today', 'Overdue') wrap the assignee avatar onto a second meta line by itself (Navigation keyboard support, Search API pagination at 1280; again at 768 and 375 in L2/L6), so those cards are taller and ragged next to their neighbours.
- **S2** index.html · 768×1024 · full page, 1 screen(s) ([screens](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#S2))
  - 👁 **L2**: Tablet 768x1024, one screen. The header keeps the desktop arrangement (search narrowed to 220px next to "New task"), and the board has switched to the sideways row: Backlog and In progress at 288px each, Review cut by the right edge at "Navigation key…" / "Broken logo o…" — the peek is the only sign the row scrolls, there is no scrollbar or arrow in the picture. Cards and chips are identical to desktop. Same avatar crowding as desktop: five 32px circles overlapping so the initials touch ("AK MR PN JW LO" as one run). "Search API pagination" again drops JW onto its own row below "Urgent · 4/5 · Overdue". The board ends at y≈890; the rest of the screen is empty canvas, which is fine for a board this size. Nothing overflows the page horizontally; the only cut content is inside the board's own scroller.
- **S3** index.html · 375×812 · full page, 2 screen(s) ([screens](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#S3))
  - 👁 **L3**: Mobile 375x812, two screens (1149px). The phone layout is the weak spot. The app bar stacks into three rows: "Sprintboard", then the search field at full width, then "New task" alone on a third row at the left — the search field was meant to share its row with the button and pushed it down instead. The avatars under the title overlap so much that the initials are cut: I read "AI", "MI", "PN", "JV", "LO" — only the last circle shows its letters whole. The chips wrap to two lines (All · Design · Frontend / Backend · Bug), then "Showing all 14 cards", then the segmented column switcher (Backlog current in white, In progress, Review, Done — neat). Added up, the header is ~480px of chrome: the first card starts at y≈490 on an 812px screen, so a first-time visitor sees more controls than board. The board itself works: the Backlog column is ~315px wide with a 16px sliver of In progress peeking at the right edge (blue dot visible), cards fit their meta on one line at this width, and screen 2 shows the full Backlog column ending with "Add a card". No horizontal page overflow visible.
    - ✗ **D4** defect `.appbar .search @375`: On phones 'New task' drops to its own third row under a full-width search field instead of sharing the search row — the field's intrinsic width plus the button exceed the 343px row.
    - ✗ **D5** defect `header.app-header @375`: Phone header is ~480px of chrome: brand, search, New task, title, avatars on their own row, two rows of chips, a 'Showing all 14 cards' line and the switcher — the first card starts at y≈490 of 812, so the board is below the fold's midpoint.
- **S4** index.html · 1280×800 · element #card-SB-102 · @2x — card close-up: meta row (screens not kept)
  - 👁 **L4**: 2x close-up of "Footer links 404 on /press". Top row: red "Bug" and blue "Frontend" chips, ⋯ at the top-right corner with clear space from the chips. Title in bold near-black. Meta row: "High" with a bar icon, "1/3" with a checkbox icon, a pink "Overdue" chip with a clock icon, and the orange "PN" avatar pushed to the right edge. At 2x the priority icon is clearly wrong: all three bars are the same pale peach, although this card is High and should show three solid orange bars — so every priority level renders the same icon. Alignment: the text baselines of High / 1/3 / Overdue line up and the avatar is centred on the row, so the row looks centred-aligned as intended; but the boxes are three different heights (plain text ~17px, the Overdue chip 22px, the avatar 24px), which is the 2–3.5px top-edge spread integrity G1 measured. Visible consequence: when the row wraps (S1, Navigation keyboard support) the second line sits at an uneven distance.
    - ✗ **D3** defect `.prio svg .bar`: The priority icon draws every bar at 25% opacity whatever the level: High shows three pale peach bars, the same as Low and Medium, so the icon carries no information (the rule meant to fill the bars loses on specificity to '.prio svg .bar').
- **S5** index.html · 1280×800 · element .team · @2x — team avatars close-up (screens not kept)
  - 👁 **L5**: 2x close-up of the header's team list. Five 32px circles — violet AK, teal MR, orange PN, green JW, pink LO — each with a 2px canvas-coloured ring, overlapping by 6px. Even at 2x the letters crowd the next circle: the K of AK touches MR's ring, the W of JW touches LO's, and the gaps between the pairs are ~1px, so at 1x the initials read as one run of ten letters. Colours are distinct and white-on-colour contrast looks strong; it is purely the overlap that makes the names hard to read.
    - ✗ **D1** defect `.team .avatar`: Team avatars overlap by 6px (8px on phones) so the initials collide with the next circle: at 1x the header reads as one run 'AKMRPNJWLO', and on the phone (L3) the first four are cut to 'AI', 'MI', 'PN', 'JV'.
- **G2** `vlmkit check copy index.html --manifest copy.txt` → exit 0 — missing 0 · placeholders 0 · manifestLines 9 · statesExplored 14 · revealedOnly 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G2))
- **G3** `vlmkit check design index.html` → exit 0 — verdict: COHERENT (1 finding(s)) ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G3))
  - ✗ **D7** defect `.board-head, .filter-bar`: Two spacing values sit just off the page's own scale: board-head padding-top 22px next to the 24px used 9x, filter-bar padding-bottom 18px next to 16px.
- **G4** `vlmkit check composition index.html` → exit 0 — verdict: COMPOSED (0 finding(s)) ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G4))
- **G5** `vlmkit check color index.html` → exit 0 — verdict: CONSISTENT (0 finding(s)) ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G5))
- **G6** `vlmkit check a11y contrast index.html` → exit 0 — inspected 183 · failures 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G6))
- **G7** `vlmkit check a11y focus index.html` → exit 1 — steps 40 · findings 1 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G7))
- **G8** `vlmkit check a11y touch index.html` → exit 0 — level AA · inspected 39 · failures 0 · wcagExempt 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G8))
- **G9** `vlmkit check a11y focus index.html --viewport 1280x1100` → exit 0 — steps 40 · findings 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G9))
- **G10** `vlmkit check a11y focus index.html --output-dir test-results/focus-720` → exit 1 — steps 40 · findings 1 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G10))
- **G11** `vlmkit scan scroll index.html` → exit 0 — containers 0 · overflowX 0 · issues 20 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G11))
- **G12** `vlmkit scan scroll index.html --viewport 375x812` → exit 0 — containers 1 · overflowX 0 · issues 20 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G12))
- **G13** `vlmkit scan handlers index.html --probe-drag` → exit 1 — registrations 175 · elements 69 · suspects 8 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G13))
  - ✗ **D8** defect `.card-item.is-dragging .card`: While a card is dragged its source copy is faded to opacity .45 and becomes illegible — G13 measures 15.7:1 at rest dropping to ~3.0:1 mid-drag on SB-101..104. The source position should be shown as a placeholder, not as washed-out text.
  - ✗ **D9** defect `.card (dragend)`: A drag cancelled with Escape does not leave the card as it was: G13 finds 15–27% of the card's box still different after dragend (SB-101..104). Likely the 180ms opacity transition on .card still easing back when the drag ends.
- **G14** `vlmkit check interactions index.html --max-elements 60` → exit 0 — elements 44 · suspects 0 · warns 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G14))
  - ✗ **D10** defect `.card[role=button]`: Cards open a modal dialog on Enter/click but do not say so (no aria-haspopup=dialog), so a screen reader hears a plain button — and G14 logs only 'Enter: focus moves within' for every card, never probing the dialog's focus trap and return that the brief requires.
- **G15** `vlmkit check motion index.html` → exit 0 — activeAnimations 0 · running 0 · issues 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G15))
- **G16** `vlmkit check animation index.html` → exit 0 — animations 0 · settleMs 0 · issues 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G16))
  - ✗ **D11** defect `.card settle / check animation`: The brief's drop settle (≤200ms, removed by reduced motion and ?animate=0) is unmeasured: check animation found 'animations: 0' because the settle only runs after a drop and the page is still on load, and the settle was a transition trick that leaves nothing in document.getAnimations() once it ends — so no gate can say it has a visible effect, how long it takes, or that reduced motion removes it.
- **G17** `vlmkit check scroll index.html --viewport 375x812` → exit 0 — pageScrolled 337 · stickyFixed 0 · engagedSticky 0 · snaps 1 · issues 1 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G17))
- **G18** `vlmkit check breakpoints index.html --sweep` → exit 0 — breakpoints 3 · issues 0 · sweepRanges 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G18))
- **G19** `vlmkit check grounding index.html` → exit 0 — targets 37 · scale 0.5 · issues 4 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G19))
- **G20** `vlmkit check grounding index.html --json` → exit 0 — targets 37 · scale 0.5 · issues 4 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G20))
- **S6** index.html · 375×812 · scroll-to #col-review · wait 300 — phone snapped to the third column (Review) (screens not kept)
  - 👁 **L6**: Phone 375x812 after scrolling the board to the third column (window scrolled down to the board top). Snapping works: the Review column's left edge sits exactly on the 16px gutter, a 4px sliver of In progress shows at the left edge and the Done column peeks ~30px on the right, so there is always a hint of the neighbours. The segmented switcher above has moved its white "current" pill to "Review" on its own, following the scroll. Review shows its "2" count, the two cards and "Add a card". Same meta-row wrap as desktop: "Navigation keyboard support" puts MR alone on a second line under "Medium · 5/5 · Due today". The Review panel runs on empty to the bottom of the screen because every column is stretched to the Backlog's height — acceptable, it reads as the column's drop area. This picture is the evidence against G17's snap-not-snapping: the browser lands columns on their snap edge measured with the board's 16px scroll-padding.
- **N2** false-positive about G7: The one [reverse] step (SB-104's ⋯ at x=275 → card SB-105 at x=35, 'within the same row') is an artifact of scrolling, not a focus-order problem. G10's step table shows SB-104 at y=597, its ⋯ at y=603, then SB-105 at y=594 — but SB-105 is the NEXT card DOWN in the Backlog column, ~115px lower on the page (S1). Focusing it scrolled the 720px viewport, so the gate compared a pre-scroll y with a post-scroll y. G9 re-ran the same walk at 1280x1100, where nothing needs to scroll: 0 findings. The order card → its ⋯ → next card is the page's reading order.
- **N3** false-positive about G10: Re-run of G7 into its own output dir to read the step coordinates; same scroll artifact, see the note on G7.
- **N4** tool about G7: check a11y focus compares focus-stop positions in viewport coordinates across a focus-induced scroll. Verbatim: '[reverse] Focus moved left within the same row (from main#board>section#col-backlog>ol.card-list>li.card-item>button.card-menu-btn at x=275 to main#board>section#col-backlog>ol.card-list>li.card-item>div#card-SB-105 at x=35)'. Report rows: '| 11 | …div#card-SB-104 | … | 35, 597 |', '| 12 | …button.card-menu-btn | … | 275, 603 |', '| 13 | …div#card-SB-105 | … | 35, 594 |'. SB-105 is 115px below SB-104 in document coordinates; adding scrollY to each step (or comparing document positions) would remove the finding. At 1280x1100 (G9) the same page has 0 findings.
- **N5** false-positive about G11: All 20 clipped-content warns (and the identical 20 at 375 in G12) are the page's .sr-only spans — ' priority', ' checklist items done', ', was due Mon, Sep 21, 2026', 'Assigned to Priya Nair', 'Due ', the chips' legend — 1px boxes with overflow hidden + clip by design, so the card's accessible description reads 'High priority, 1/3 checklist items done, Assigned to Priya Nair' while the visible row stays compact. Integrity (G1) lists the very same selectors under 'exempted … clipped away (overflow is not visible)'. The 5 'dead scrollports' are the four hidden add-card textareas (overflow:auto is the UA default for textarea) and the closed dialog, whose overflow:auto is a guard for content taller than a short screen — both listed, not counted as warns.
- **N6** false-positive about G12: Same 20 .sr-only spans as G11 at 375x812; see the note on G11. The part of G12 that matters is right: page overflow-x 0 and exactly one scroll container, #board axis=x overflow=953px — the snapped phone row.
- **N7** tool about G11: scan scroll reports the standard visually-hidden pattern as cut-off content while check integrity on the same page exempts it. Verbatim: '! clipped-content #meta-SB-103 > span:nth-of-type(5): #meta-SB-103 > span:nth-of-type(5) clips 173px of its content behind overflow: hidden — cut-off content unless the oversize is decorative.' (that span is class sr-only, text 'Assigned to Ana Kowalski'). G1 for the same element family: '[text-collision] #meta-SB-102 > span:nth-of-type(5) @1280: clipped away by … (overflow is not visible)' under Exempted. The two gates disagree about one element.
- **N8** false-positive about G17: snap-not-snapping is wrong about this page. The board has padding-inline 16px and scroll-padding-inline 16px, so each column's snap position is its offset minus 16: 0, 327, 654, 981 (the last clamps to the 953px maximum). The gate's 'nearest candidates: 16, 343, 670, 997px' are the raw column offsets without scroll-padding, and 'settled at 0px' is precisely the first column's snap position. S6/L6 shows the browser landing the Review column exactly on the 16px gutter after a scroll, with the switcher following.
- **N9** tool about G17: check scroll ignores scroll-padding when it judges where a mandatory snap container settled. Verbatim: '! snap-not-snapping #board: #board declares scroll-snap-type x mandatory but settled at offset 0px, aligned to no child snap edge (nearest candidates: 16, 343, 670, 997px) — children may be missing scroll-snap-align.' The same line also reports '4 snap-aligned children', so the closing hint contradicts its own count. #board has scroll-padding-inline: 16px; 16 − 16 = 0.
- **N10** accepted about G19: The three imprecise-target warns are the bottom edge of the gate's 1280x720 frame, not small targets: SB-105's card and ⋯ and Done's 'Add a card' are cut to 5px and 2px because the board runs past 720px — the gate says itself it 'measures the initial frame and never scrolls'. Their real sizes (card 137x63, ⋯ 14x14, add button 137x18 screenshot px) clear the 10px floor. ambiguous-target (three 'Add a card' at the foot of In progress, Review, Done) is accepted: each sits directly under its own column header, the accessible names are already unique ('Add a card to Review', etc. via aria-label), and 'Add a card' is required copy that must stay verbatim; lengthening the visible text would crowd 250px columns.
- **N11** tool about G19: check grounding's ambiguous-target advice asks for an accessible name the page already has. Verbatim: '! ambiguous-target: 3 buttons in the frame all read "Add a card" (t23 at 242,321, t29 at 398,264, t39 at 554,358) — distinguishable only by their surrounding text (…), so an accessible name carrying it would let an agent name one.' Each of those buttons has aria-label="Add a card to In progress" / "… to Review" / "… to Done"; the --json map labels them 'Add a card' (visible text), while the icon-only + buttons in the same columns are labelled by their aria-label ('Add a card to Review'). The label source differs by whether visible text exists, and the advice does not check the name it recommends.
- **N12** accepted about G13: unprobed-handler-types (warn) is this gate's own disclaimer that it presses nothing: blur/change/click/close/focus/focusout/input/keydown/scroll/search/submit are exercised by check interactions (G14: every card, menu and radio probed) and by the state shots in the interactions round.
- **F1** fix for D1: Team avatars no longer overlap: side by side with a 4px gap and no canvas ring (styles.css .team / .avatar); phones keep 28px circles with the same gap.
- **F2** fix for D2: Moved the assignee avatar out of the meta row into a new .card-top row beside the labels (margin-left:auto, left of the ⋯ corner button, which moved to top:8px to centre on that row). The meta row now holds only priority · checklist · due. 'Assigned to …' stays as sr-only text in the described meta (app.js renderCard).
- **F3** fix for D3: Priority bars: unlit bars use a neutral --bar-off grey, lit bars currentColor; the lit-bar selectors now carry the same specificity as the base rule (.prio--high svg .bar) and come after it, so Low/Medium/High show 1/2/3 coloured bars.
- **F4** fix for D4: Phone search field is flex: 1 1 0 with min-width 0, so it takes whatever the New task button leaves on the second app-bar row instead of pushing the button to a third row.
- **F5** fix for D5: Phone header compacted: New task shares the search row (D4), board-head/filter paddings 16/12px, and the unchanging 'Showing all 14 cards' line is visually hidden on phones while no filter is active (.filter-summary.is-idle, still in the accessibility tree as the live status; it reappears when a filter or search is applied).
- **F6** fix for D6: Every .meta item is a 22px-high inline-flex box (chips and plain items alike), so the row shares one top edge; the 24px avatar left the row in D2.
- **F7** fix for D7: Snapped board-head padding-top 22→24px and filter-bar padding-bottom 18→16px onto the page's scale.
- **F8** fix for D8: The dragged card's slot is now a placeholder — accent-soft fill with a 2px dashed indigo outline and its contents visibility:hidden — instead of the card at opacity .45. The browser's drag image (taken before the class is added, one frame later) is what travels with the pointer.
- **F9** fix for D9: Removed opacity from .card's transition list (it now transitions transform and box-shadow only, the settle), so removing .is-dragging in dragend restores the card in the same frame whether the drag dropped or was cancelled.
- **F10** fix for D10: Cards carry aria-haspopup="dialog" (app.js buildItem): a screen reader announces that Enter opens a dialog, and check interactions can recognise the card as a popup trigger and probe the trap and focus return.

### R2 · fix pass (builder)

- **S7** index.html · 1280×800 · full page, 2 screen(s) ([screens](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#S7))
  - 👁 **L7**: Desktop 1280 after the fix pass, two screens (942px). Team list top-right is now five separate circles — AK, MR, PN, JW, LO each fully readable, 4px apart, no letters touching. Every card has the same three-row anatomy: labels at the left of the top row with the assignee avatar at its right end beside the ⋯, the bold title, then one meta line of priority · checklist · due. "Navigation keyboard support" (Medium · 5/5 · Due today) and "Search API pagination" (Urgent · 4/5 · Overdue) now fit on one line and are the same height as their neighbours, so the columns read as even stacks. The priority icon finally encodes the level: Low shows one slate bar and two light grey ones, Medium two amber-orange, High three orange, Urgent the red square with "!". Chips, due pills and plain items share one vertical centre and top edge. The avatar sits ~8px left of the ⋯; both are clearly separate targets. Header, filter bar and columns are otherwise unchanged; no overlap, clipping or wrap problems anywhere.
- **S8** index.html · 375×812 · full page, 2 screen(s) ([screens](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#S8))
  - 👁 **L8**: Mobile 375 after the fix pass, two screens (1074px, was 1149). The app bar is two rows: "Sprintboard", then the search field and the indigo "New task" button side by side. Below: "Website relaunch", the caption, and the five avatars in a row with every initial whole. The chips still take two lines (All · Design · Frontend / Backend · Bug); the "Showing all 14 cards" line is gone from the idle phone view. The column switcher follows, and the Backlog column header now starts at y≈365 with the first card at y≈402 (was ≈490) — the board begins well above the fold's midpoint. Cards show the new top row (labels, avatar, ⋯) and a single meta line. The In progress column still peeks at the right edge. Remaining nit: the second chip row holds only two chips, a little lopsided but honest — nothing is hidden or scrolled away.
- **G21** `vlmkit check integrity index.html` → exit 0 — verdict: CLEAN (0 fail, 0 warn, 294 exempted) ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G21))
- **G22** `vlmkit check design index.html` → exit 0 — verdict: COHERENT (0 finding(s)) ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G22))
- **G23** `vlmkit scan handlers index.html --probe-drag` → exit 1 — registrations 175 · elements 69 · suspects 8 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G23))
- **G24** `vlmkit scan handlers index.html --probe-drag --json` → exit 1 — registrations 175 · elements 69 · suspects 8 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G24))
- **G25** `vlmkit scan handlers index.html --probe-drag` → exit 1 — registrations 175 · elements 69 · suspects 4 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G25))
- **G26** `vlmkit scan handlers 'file:///home/user/vlmkit/examples/sites/kanban/index.html?animate=0' --probe-drag` → exit 0 — registrations 175 · elements 69 · suspects 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G26))
- **G27** `vlmkit check interactions index.html --max-elements 60` → exit 0 — elements 44 · suspects 0 · warns 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G27))
- **V1** D1 verified by L7: Desktop: five separate, fully readable circles 4px apart; L8 shows the same on the phone.
- **V2** D2 verified by L7: Navigation keyboard support and Search API pagination now keep priority · checklist · due on one line; avatar in the top row; card heights even.
- **V3** D3 verified by L7: Low = 1 slate bar, Medium = 2 amber bars, High = 3 orange bars, Urgent = red square — the icon now encodes the level.
- **V4** D4 verified by L8: Phone app bar is two rows: brand, then search + New task side by side.
- **V5** D5 verified by L8: First card at y≈402 on the phone (was ≈490); page 1074px (was 1149).
- **V6** D6 verified by G21: check integrity: CLEAN, 0 warn — the 8 meta-row near-misalignments are gone.
- **V7** D7 verified by G22: check design: COHERENT, 0 findings; no scale-outlier line.
- **V8** D10 verified by G27: Every card now reads 'Enter: … | opens dialog (focus enters), traps | Esc closes+returns focus' — the dialog's trap and focus return are probed and pass.
- **F11** fix for D8: Second pass after G23: the blank dashed slot still measured 15.6:1 → 1.8:1 'at opacity 1' — a slot with no text at all has nothing legible to measure. The placeholder now keeps the card's title visible in --ink-2 (≈6.9:1 on the accent-soft fill) while labels, meta, avatar and ⋯ are hidden, so the slot says what is leaving and stays readable.
- **N13** superseded about D9: D9 was misdiagnosed. F9 (dropping opacity from .card's transition) is harmless but did not change the finding: G23 and G25 still report 21–25% after Escape. G26 ran the same probe on index.html?animate=0 — the page's switch that removes the drop settle — and reports 0 suspects: after a cancelled drag the card is exactly as it was. The probe drives real drops first (G24's JSON timeline shows SB-101 dropped into Review before the cancel gesture), each drop starts the 180ms settle, and the cancel gesture's 'before' picture is taken while that settle is still easing. The cancel path (dragend removes .is-dragging and the drop UI) was never the problem, so there is no page defect left under D9.
- **N14** false-positive about G25: The 4 drag-cancel-not-reverted suspects here are the probe's timing, not the page: G26 is the identical probe with the drop settle switched off (?animate=0) and has 0 suspects, and the brief requires that settle (≤200ms). The probe drops each card for real, then starts its Escape-cancel gesture within the 180ms settle of that drop, so its baseline is a mid-transition frame and the at-rest card afterwards 'differs'. A user cancels seconds after a drop, never inside 180ms.
- **N15** tool about G25: scan handlers --probe-drag takes the cancel gesture's baseline without waiting for the page's own transitions from the PREVIOUS gesture (a real drop) to finish. Verbatim: 'suspect [drag-cancel-not-reverted] section#col-backlog>ol.card-list>li.card-item>div#card-SB-101 "BackendJWAudit legacy URL redirectsMediu" was dragged, Escape was pressed, and the browser cancelled the drag — dragend reported dropEffect "none" and no drop ran — but 21% of the element's own box still differs from before the gesture.' Same probe on ?animate=0 (G26): no issue. Waiting for document.getAnimations() to settle before each baseline would separate a stranded optimistic change from a transition still in flight. Also noted: after F8's first pass the rule read a deliberate blank placeholder (no text, opacity 1) as '15.62:1 at rest, 1.77:1 mid-flight at opacity: 1' — the placeholder it recommends ('a dashed outline, a gap, a placeholder') still fails it unless the slot keeps legible text.
- **N16** superseded about G24: JSON re-run of the probe to read its timeline (it showed the real drops before the cancel gesture); taken before F8's second pass, superseded by G25/G26.
- **N17** superseded about G23: Probe after F8/F9 first pass; its ghost finding drove F8's second pass and its cancel finding is explained in the D9 note. Superseded by G25/G26.
- **G28** `vlmkit scan handlers index.html --probe-drag` → exit 1 — registrations 175 · elements 69 · suspects 4 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G28))
- **V9** D8 verified by G28: G28 (after F11): no drag-ghost-illegible on any of the five driven cards and no mid-drag legibility drop in the gesture lines — the titled placeholder keeps the slot readable.
- **N18** false-positive about G28: Same 4 drag-cancel-not-reverted suspects as G25, same cause: the probe's cancel baseline lands inside the 180ms settle of the real drop it just made. The identical probe with the settle off (G26, ?animate=0) has 0 suspects. See the D9 and G25 notes.
- **N19** superseded about G25: Taken between the placeholder change and its fix record (F11); G28 is the same probe after F11.
- **V10** D9 verified by G26: With the drop settle switched off (?animate=0) a cancelled drag leaves the card exactly as it was: 0 suspects. This verifies the cancel path, not F9 — F9 was not the cause; the remaining G25/G28 suspects are the probe baselining inside the previous drop's 180ms settle (N13, N14).

### R3 · interactions and motion (builder)

- **F12** fix for D11: The settle is now a 180ms @keyframes animation (sb-settle, transform from translateY(-6px) rotate(-1.5deg) scale(1.03) to none, fill both) restarted on every pointer drop, menu move and added card; the keyboard lift moved to the individual rotate/scale properties so a finished settle cannot pin it. Reduced motion and .no-motion set animation:none. New documented parameter ?replay=drop runs one real drop on load (Hero copy → Review, position 2 of 3) through place → afterMove → settle, so check animation has something to sample; without it nothing moves on load.
- **G29** `vlmkit check animation 'file:///home/user/vlmkit/examples/sites/kanban/index.html?replay=drop'` → exit 0 — animations 2 · settleMs 180 · issues 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G29))
- **G30** `vlmkit check animation 'file:///home/user/vlmkit/examples/sites/kanban/index.html?replay=drop&animate=0'` → exit 0 — animations 0 · settleMs 0 · issues 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G30))
- **G31** `vlmkit check animation 'file:///home/user/vlmkit/examples/sites/kanban/index.html?replay=drop' --samples 6 --strip /tmp/claude-0/-home-user-vlmkit/663fea0b-e0f8-5798-a548-7572e9f0176c/scratchpad/settle-strip.png` → exit 0 — animations 2 · settleMs 180 · issues 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G31))
- **V11** D11 verified by G29: check animation on ?replay=drop: 2 animations on #card-SB-106 (sb-settle, sb-settle-shadow), both 'visible', settle 180ms (≤200ms), 'reduced-motion: honored'. G30 (?replay=drop&animate=0): animations 0 — the switch removes it.
- **N20** decision about G31: Filmstrip of the settle, 6 samples over 180ms (written to the scratchpad, read by eye): at 30ms Hero copy is raised ~5px and tilted with the deep lift shadow, at 60–90ms it is nearly flat, at 120–180ms it rests flush in Review with the normal card shadow. The motion is small and quick — a landing, not a bounce. check animation is run on ?replay=drop from here on, because the plain page has no motion on load by design.
- **S9** index.html · 1280×800 · focus #card-SB-106 · press Space · press ArrowRight · press ArrowDown · wait 250 — keyboard: Hero copy picked up, moved right then down (screens not kept)
  - 👁 **L9**: Desktop, keyboard move in progress: Space on Hero copy, → then ↓. Hero copy now sits in Review between "Navigation keyboard support" and "Broken logo on older Safari", visibly picked up — tilted ~1.5°, scaled up a touch, deep shadow, 2px indigo outline. The Review column is tinted indigo with an indigo border as the target, its count already reads 3, and In progress reads "WIP 2 / 3" — counts follow the move live. Nothing else on the board shifted. At the bottom a dark hint bar says "Moving ‘Hero copy’ · Review, position 2 of 3" with key caps ← → column, ↑ ↓ position, Space drop, Esc cancel — the right content, but the bar is only ~640px wide on a 1280 screen, so the status breaks after "Review," and "Esc cancel" falls to a second line: it reads as cramped rather than as one calm instruction strip. It also sits over the lower part of the Backlog/In progress/Review columns while visible.
    - ✗ **D12** defect `#kb-hint`: The keyboard hint bar is squeezed to ~640px on a 1280px screen (a fixed box at left:50% shrink-fits to the half of the viewport right of its left edge), so its status and key list each break onto two lines.
- **S10** index.html · 1280×800 · focus #card-SB-104 · press Space · press ArrowDown · wait 250 — keyboard: Newsletter signup moved to the bottom of Backlog (screens not kept)
  - 👁 **L10**: Desktop, Space then ↓ on Newsletter signup endpoint: it is now Backlog position 5 of 5, lifted with the indigo outline, and the page scrolled 47px to follow it — but only just: the card's bottom edge sits on the viewport's bottom edge and its tilted lower corner is cut off by the screen, so the card being carried is the one thing not fully in view. The hint bar ("Moving ‘Newsletter signup endpoint’ · Backlog, position 5 of 5", keys on two lines again) occupies x≈320–960 at the same height; here the Backlog card is left of it, but a card taken to the bottom of In progress or Review would be scrolled to exactly the band the bar covers.
    - ✗ **D13** defect `.card-item (scrollIntoView while lifted)`: While moving by keyboard, the carried card is scrolled only until it touches the viewport edge: at the bottom of a column its tilted corner is cut off by the screen, and in the middle columns it would land under the fixed hint bar.
- **F13** fix for D12: #kb-hint gets width: max-content (still capped at 100vw − 32px), so on desktop the status and the four key groups sit on one line; the phone layout keeps its full-width two-row bar (width:auto there).
- **F14** fix for D13: Card items carry scroll-margin 12px 12px 112px, which scrollIntoView honours: a card carried to the bottom of a column is revealed with 112px below it — clear of the screen edge and of the ~62px hint bar at bottom:20px.
- **S11** index.html · 1280×800 · focus #card-SB-104 · press Space · press ArrowDown · wait 250 — keyboard: Newsletter signup to the bottom of Backlog (after D12/D13) (screens not kept)
  - 👁 **L11**: Desktop, same move as S10 after D12/D13. The page now scrolls further (the header is off-screen, the filter chips at the top) so Newsletter signup endpoint — lifted, tilted, indigo outline, Backlog tinted as the target — sits fully in view with the Backlog's "Add a card" and ~30px of column below it and the hint bar below that. The hint bar is a single line centred at the bottom: "Moving ‘Newsletter signup endpoint’ · Backlog, position 5 of 5" followed by ← → column, ↑ ↓ position, Space drop, Esc cancel, all with key caps; nothing wraps and nothing on the carried card is covered.
- **S12** index.html · 1280×800 · focus #card-SB-107 · press Space · press ArrowRight · press ArrowDown · press ArrowDown · wait 250 — keyboard: Pricing page layout to the bottom of Review (screens not kept)
  - 👁 **L12**: Desktop, Pricing page layout carried → and ↓↓ into Review: it is Review position 3 of 3, below Broken logo on older Safari, lifted with the indigo outline, Review tinted and counting 3, In progress now "WIP 2 / 3". The second ↓ at the bottom did nothing visible (the live region says it is already at the bottom), which is the right behaviour. The card sits wholly above the one-line hint bar ("Moving ‘Pricing page layout’ · Review, position 3 of 3 …"); the bar only overlaps the Done column's "Add a card" and the lower edge of the Backlog's last card while the move is in progress.
- **V12** D12 verified by L11: One-line hint bar at 1280, centred; no wrap in status or keys (also L12).
- **V13** D13 verified by L11: Carried card at the bottom of Backlog is fully visible above the hint bar with room to spare; L12 shows the same in the middle column (Review).
- **S13** index.html · 1280×800 · focus #card-SB-106 · press Space · press ArrowRight · press ArrowDown · press Space · wait 400 — keyboard: Hero copy dropped in Review, position 2 (screens not kept)
  - 👁 **L13**: Desktop after Space, →, ↓, Space on Hero copy. The card has settled flat into Review position 2 — no tilt or lift shadow left, just the 2px indigo keyboard focus ring — between Navigation keyboard support and Broken logo on older Safari. Review counts 3, In progress "WIP 2 / 3", and the Review column's target tint is gone. The hint bar at the bottom switched to its idle form: "Keyboard · Space pick up · Enter open · Shift F10 menu", one line with key caps. The announcement is in the (invisible) live region; on screen nothing else changed, which is what a drop should look like.
- **S14** index.html · 1280×800 · click li[data-id='SB-102'] .card-menu-btn · wait 150 — card menu open on Footer links 404 (screens not kept)
  - 👁 **L14**: Desktop, ⋯ clicked on Footer links 404 on /press. A white popover with a soft shadow opens under the button, right-aligned to it: "Open card" (first item, highlighted in pale indigo), a divider, the small caps label "MOVE TO…", then Backlog (grey dot, greyed text, "Current" at the right), In progress (blue dot), Review (amber dot), Done (green dot). The ⋯ button shows its pressed grey background. The popover covers the next two Backlog cards, as a menu should; its left edge leaves the start of the underlying title ("Foot…") visible, which reads as layering, not as a collision. Type sizes and spacing match the rest of the app.
- **S15** index.html · 1280×800 · click li[data-id='SB-109'] .card-menu-btn · click .menu-item[data-to='progress'] · wait 400 — In progress over its WIP limit (moved via Move to…) (screens not kept)
  - 👁 **L15**: Desktop after ⋯ → Move to… In progress on Navigation keyboard support. In progress turns light red with a red border, its pill reads "WIP 4 / 3" in red, and a white alert with a red border and warning triangle sits under the header: "Over the WIP limit by 1 card. Finish something before starting more." The moved card is last (position 4) with its "Due today" chip intact; Review now shows 1. The state is unmistakable and stated in words, as the brief asks. Weakness: the alert is one undifferentiated red paragraph broken over three lines ("…1 card. / Finish something before / starting more.") — the fact and the advice have the same weight, so the column header area feels heavy; the fact should lead and the advice sit quieter.
    - ✗ **D14** defect `.wip-alert`: The over-limit alert is a single three-line red paragraph at one weight: the status ('over by 1 card') and the advice read the same, and the block feels heavy under the header.
- **F15** fix for D14: The alert is now two lines of different weight: 'Over the WIP limit by 1 card' in bold red leads, and 'Finish something before starting more.' follows on its own line in --ink-2 at weight 500 (app.js updateColumns, styles.css .wip-alert strong / .wip-advice).
- **S16** index.html · 1280×800 · element #col-progress · click li[data-id='SB-109'] .card-menu-btn · click .menu-item[data-to='progress'] · wait 400 — In progress over its limit, alert close-up (after D14) (screens not kept)
  - 👁 **L16**: Close-up of In progress after the same over-limit move, with F15 in. The alert now leads with "Over the WIP limit by 1 card" in bold red beside the triangle, and the advice sits under it in grey at normal weight — the hierarchy is right and the block is lighter. New flaw from the change: the advice wraps as "Finish something before starting" / "more." — a one-word orphan on the last line of a two-line note. Header pill "WIP 4 / 3" in red, the red column tint and the four cards (Navigation keyboard support last, "Due today" kept) are as before.
- **F16** fix for D14: Second pass after L16: .wip-advice gets text-wrap: pretty so the two-line advice never ends on a lone word.
- **S17** index.html · 1280×800 · element #col-progress · click li[data-id='SB-109'] .card-menu-btn · click .menu-item[data-to='progress'] · wait 400 — In progress over its limit, alert close-up (D14 second pass) (screens not kept)
  - 👁 **L17**: Same close-up after text-wrap: pretty. The advice now breaks as "Finish something before" / "starting more." — two balanced lines, no orphan — under the bold red "Over the WIP limit by 1 card". The alert reads fact first, advice second; the block is compact and sits evenly between the header pill "WIP 4 / 3" and the first card.
- **V14** D14 verified by L17: Bold fact line, quiet grey advice on two balanced lines, no orphan.
- **S18** index.html · 1280×800 · click #card-SB-106 · wait 200 — card detail dialog: Hero copy (screens not kept)
  - 👁 **L18**: Desktop, Hero copy clicked: a 600px white dialog, 16px radius, centred on a half-dimmed board. Reads top-down cleanly: kicker "● In progress · SB-106" in small grey, the title "Hero copy" at 21px bold, the purple Design chip, then a light grey facts panel in three columns — ASSIGNEE (violet AK circle + "Ana Kowalski"), DUE DATE "Fri, Sep 25, 2026", PRIORITY (three orange bars + "High"). Then DESCRIPTION with two lines of body text, and CHECKLIST with "2 of 5 done" right-aligned over an indigo progress bar at 40%, and five checkbox rows: the first two ticked and struck through in grey, the other three open. The × close sits top-right with generous space. Section labels share one small-caps style; spacing between sections is even. Nothing is clipped; the dialog ends 30px below the last row.
- **S19** index.html · 1280×800 · click #card-SB-106 · click #dlg-checklist li:nth-child(3) input · wait 300 — dialog: third checklist item ticked (screens not kept)
  - 👁 **L19**: Same dialog after clicking the third checkbox. "Legal review of product claims" is now ticked and struck through (its row still shows the pale hover band because the pointer is on it), the count reads "3 of 5 done" and the bar has grown to 60%. The other rows and the facts panel are unchanged, so the update is local and obvious. The card behind is covered by the dialog here; S20 shows the board side.
- **S20** index.html · 1280×800 · focus #card-SB-106 · press Enter · wait 150 · press Tab · press Tab · press Space · press Escape · wait 300 — keyboard: Enter opens Hero copy, tick an item, Escape closes (screens not kept)
  - 👁 **L20**: Keyboard round trip on the board: Enter on Hero copy opened the dialog, two Tabs moved from the close button to the second checklist row, Space unticked it, Escape closed. Back on the board Hero copy's meta now reads "1/5" (was 2/5), and focus is back on the Hero copy card — indigo ring around it and the idle hint bar "Keyboard · Space pick up · Enter open · Shift F10 menu" at the bottom. So the dialog's checklist writes through to the card, Escape closes, and focus returns to the card that opened it, as the brief asks.
- **S21** index.html · 1280×800 · click input[name='label-filter'][value='design'] · wait 200 — filter: Design (screens not kept)
  - 👁 **L21**: Desktop, Design chip clicked. The Design chip turns dark with its purple dot ringed in white, All goes back to outline — the selected filter is obvious. Right of the chips: "Showing 5 of 14 cards" and an underlined "Clear filters". The board keeps its four columns and their total counts (5, WIP 3 / 3, 2, 4) and shows only Design cards: Cookie banner copy review in Backlog with "4 cards hidden by the filter" under it, Hero copy and Pricing page layout in In progress with "1 card hidden by the filter", Brand colour tokens and Content inventory in Done with "2 cards hidden by the filter". Review, with no Design card, shows a dashed empty box "No cards match the filter" above its "Add a card". The board shrinks to its content and nothing jumps sideways; the notes explain why the counts are larger than what is shown.
- **S22** index.html · 1280×800 · fill #search "page" · wait 200 — search: page (screens not kept)
  - 👁 **L22**: Desktop, "page" typed into the search field (indigo focus ring, native × to clear). "Showing 1 of 14 cards · Clear filters"; only Pricing page layout remains, in In progress, with "2 cards hidden by the filter" under it; Backlog, Review and Done each show the dashed "No cards match the filter" box. The filtering is right but the words are not: no label filter is on (All is still selected) — it is the search that hid these cards, and "the filter" sends the reader to the chips to look for what they turned on.
    - ✗ **D15** defect `.column-note`: Column notes always blame 'the filter' ('No cards match the filter', '2 cards hidden by the filter') even when only a search is active and every chip is off, so the note points the reader at the wrong control.
- **S23** index.html · 1280×800 · click #col-review .add-card-btn · type Launch-day checklist · wait 200 — add-card form open in Review (screens not kept)
  - 👁 **L23**: Desktop, "Add a card" clicked in Review and "Launch-day checklist" typed. The button gives way to an inline form styled like a card: white panel, soft shadow, a small bold "Card title" label, a two-line textarea with the indigo focus ring holding the typed title, then an indigo "Add" and a ghost "Cancel" side by side. It sits right under Broken logo on older Safari in the same column width, so it is clear where the card will land. Label, field border and buttons are all legible on white; nothing else on the board moved.
- **F17** fix for D15: updateColumns names the cause: 'the search' when only a query is set, 'the filter' when only a chip is on, 'the filter and search' for both — in both the empty box ('No cards match the search') and the hidden count ('2 cards hidden by the search').
- **S24** index.html · 1280×800 · fill #search "page" · wait 200 — search: page (after D15) (screens not kept)
  - 👁 **L24**: Desktop, "page" in the search field after D15, chips on All. The notes now name the search: Backlog, Review and Done show the dashed box "No cards match the search", and under Pricing page layout In progress says "2 cards hidden by the search". Summary "Showing 1 of 14 cards · Clear filters" unchanged. The empty boxes are centred in their dashed frames and all four columns keep the same header row, so the board still reads as a board while nearly empty.
- **S25** index.html · 1280×800 · click input[name='label-filter'][value='bug'] · fill #search "logo" · wait 200 — filter Bug + search logo (screens not kept)
  - 👁 **L25**: Desktop, Bug chip on plus "logo" in the search. Bug chip is dark with its red dot ringed white; the summary says "Showing 1 of 14 cards". Review shows Broken logo on older Safari with "1 card hidden by the filter and search" beneath; the other three columns show "No cards match the filter and search" — the longest version of the note still fits one line inside the 250px dashed box. The combined cause is stated, so either control can be the one to undo.
- **V15** D15 verified by L24: Search-only states say 'the search'; L25 shows the combined wording for chip + search.
- **S26** index.html · 1280×800 · click #col-review .add-card-btn · type Launch-day checklist · press Enter · wait 400 — add a card: submitted in Review (screens not kept)
  - 👁 **L26**: Desktop, "Launch-day checklist" typed into Review's form and Enter pressed. The new card lands at Review position 3 — a bare card with just its title and "Medium", no label row, the ⋯ aligned with the title — and the form stays open under it with an empty field ("What needs doing?" placeholder) still focused, ready for the next one. Review counts 3 and the caption and summary say 15 cards. The bare card looks thinner than its neighbours, which is honest for a card nobody has filled in yet.
- **S27** index.html · 1280×800 · element #col-backlog · click #col-backlog .add-card-btn · press Enter · wait 200 — add a card: empty submit shows the error (screens not kept)
  - 👁 **L27**: Backlog close-up after opening its form and pressing Enter with nothing typed. The field keeps focus with a red border inside the indigo ring, and "Give the card a title first." appears in bold red under it, above Add and Cancel. No empty card was created (count still 5). The message is specific and sits exactly where the eye already is.
- **S28** index.html · 375×812 · focus #card-SB-106 · press Space · press ArrowRight · wait 500 — phone: Hero copy picked up and moved to Review (screens not kept)
  - 👁 **L28**: Phone 375x812: Space on Hero copy, then →. The board scrolled sideways to Review on its own and snapped, the switcher's white pill moved to "Review", and Hero copy sits lifted at the top of the tinted Review column (count 3) above Navigation keyboard support — the phone keyboard path works. The hint bar spans the bottom as a dark block, but its status breaks as "Moving ‘Hero copy’ · Review, position 1 of" / "3" — the number that matters is orphaned on its own line — and the four key groups take two more lines, so the bar is four lines tall and covers the next card (Broken logo on older Safari) up to its title.
    - ✗ **D16** defect `#kb-hint @375`: On phones the hint bar's status orphans the position count ('…Review, position 1 of' / '3') and the whole bar runs four lines, covering the card below the carried one.
- **F18** fix for D16: Hint status joins 'position N of M' with no-break spaces and uses text-wrap: balance; on phones the bar drops to 12.5px with tighter key gaps (app.js showHint, styles.css .kb-hint-status and the ≤640px .kb-hint rules).
- **S29** index.html · 375×812 · focus #card-SB-106 · press Space · press ArrowRight · wait 500 — phone: Hero copy moved to Review (after D16) (screens not kept)
  - 👁 **L29**: Phone, same move after F18. The status is one line now — "Moving ‘Hero copy’ · Review, position 1 of 3" — and the bar is three lines instead of four, so Broken logo on older Safari keeps its title visible above it. Remaining unevenness: the key row fits three groups on the first line (← → column, ↑ ↓ position, Space drop) and leaves "Esc cancel" alone on the second, so the bar ends on a stray fragment rather than a tidy block.
- **F19** fix for D16: Second pass after L29: while moving, the phone hint lays its four key groups on a 2×2 grid (← → column | ↑ ↓ position / Space drop | Esc cancel); the idle three-group hint stays a wrapping row.
- **S30** index.html · 375×812 · focus #card-SB-106 · press Space · press ArrowRight · wait 500 — phone: Hero copy moved to Review (D16 second pass) (screens not kept)
  - 👁 **L30**: Phone, same move after the second pass. The hint bar is a tidy three-line block: the one-line status "Moving ‘Hero copy’ · Review, position 1 of 3", then the keys as a 2×2 grid — "← → column" beside "↑ ↓ position", "Space drop" beside "Esc cancel" — columns aligned, no stray fragment. It stops below Broken logo on older Safari's title. The carried Hero copy at the top of the tinted Review column, the snapped board and the switcher pill on "Review" are unchanged.
- **V16** D16 verified by L30: One-line status with the count intact, keys in an even 2×2 grid; the bar no longer covers the next card's title.
- **S31** index.html · 375×812 · click #card-SB-106 · wait 200 — phone: card dialog (screens not kept)
  - 👁 **L31**: Phone, Hero copy tapped: the dialog fills the width minus 16px margins and ends at y≈740, so it fits one screen. Kicker, title, Design chip, description and the five checklist rows are all comfortable at this width. The facts panel is the weak part: its 2-column grid wraps the values unevenly — "Ana Kowalski" drops below the AK circle, "Fri, Sep 25, / 2026" breaks after the comma, and PRIORITY sits alone on a second grid row under ASSIGNEE — so the three facts read as a ragged block rather than a list.
    - ✗ **D18** defect `.dlg-facts @375`: On phones the dialog's facts panel wraps its values raggedly (name under the avatar, 'Fri, Sep 25, / 2026', Priority alone on a second grid row) instead of reading as a clean list.
- **S32** index.html · 375×812 · full page, 1 screen(s) · click input[name='label-filter'][value='design'] · wait 200 — phone: Design filter ([screens](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#S32))
  - 👁 **L32**: Phone, Design chip tapped, full page (one screen, 812px). Design is the dark chip with its ringed dot; "Showing 5 of 14 cards · Clear filters" now appears under the chips because a filter is on (it was hidden while idle). The Backlog column shows only Cookie banner copy review and "4 cards hidden by the filter", then Add a card; In progress peeks at the right edge with its two Design cards. The page shortens to one screen and nothing overflows.
- **S33** index.html · 375×812 · click li[data-id='SB-109'] .card-menu-btn · wait 400 — phone: menu on an off-screen column's card (screens not kept)
  - 👁 **L33**: Phone, ⋯ tapped on Navigation keyboard support while the board was still on Backlog (the judge's second click, on "Move to… In progress", timed out twice). The picture shows why: the board has scrolled and snapped to Review to bring the button into view, the ⋯ has its pressed grey background — and there is no menu at all. The menu opened and was dismissed straight away by the scroll the tap itself caused.
    - ✗ **D17** defect `#board scroll → closeMenu`: On phones a tap on ⋯ in a column that is not snapped into place opens the menu and closes it in the same moment: the tap scrolls the board to bring the button into view (or a snap is still settling after a swipe), and every board scroll event closes the menu. S34 shows the menu working once the board is at rest.
- **S34** index.html · 375×812 · scroll-to #col-review · wait 600 · click li[data-id='SB-109'] .card-menu-btn · wait 400 — phone: board already on Review, then ⋯ tapped (screens not kept)
  - 👁 **L34**: Phone, board first scrolled to Review, then ⋯ tapped on Navigation keyboard support. The menu opens and stays: a white popover right-aligned under the ⋯ with "Open card", the MOVE TO… label and Backlog / In progress / Review (greyed, "Current") / Done, each with its column dot, all within the 375px width. So the menu itself works on a phone; S33's failure is the dismiss-on-board-scroll rule firing on a scroll that the tap started.
- **F20** fix for D17: Board scroll no longer closes the menu unconditionally: positionMenu() (split out of openMenu) re-anchors it under its ⋯ on every board scroll, and it closes only when the button has left the board's visible width (triggerInBoard).
- **F21** fix for D18: At ≤640px the dialog facts are a one-column list: each fact a flex row with the small-caps label at the left and the value right-aligned.
- **S35** index.html · 375×812 · click li[data-id='SB-109'] .card-menu-btn · click .menu-item[data-to='progress'] · wait 500 — phone: In progress over the limit via Move to… (after D17) (screens not kept)
  - 👁 **L35**: Phone, ⋯ tapped on Navigation keyboard support from the Backlog view, then Move to… In progress — the same two clicks that timed out in S33 now go through. The board has followed the move to In progress (switcher pill on "In progress"), the column is red with "WIP 4 / 3", and the alert reads "Over the WIP limit by 1 card" in bold red over the grey "Finish something before starting more." (one line at this width). Navigation keyboard support is last, with its Due today chip; the Review column peeks at the right.
- **S36** index.html · 375×812 · click #card-SB-108 · wait 200 — phone: dialog for an overdue card (after D18) (screens not kept)
  - 👁 **L36**: Phone dialog for Search API pagination (overdue, urgent) after D18. The facts are now a list: ASSIGNEE — green JW circle and "Jonas Weber" right-aligned on one line; PRIORITY — red square "Urgent" on one line; DUE DATE — "Tue, Sep 22, 2026" with the red "Overdue by 1 day" chip right-aligned beneath it. Better than the ragged grid, but the label itself now breaks: "DUE" / "DATE" stacks into two lines beside the two-line value, the only label on the panel that wraps. Description and the 4-of-5 checklist below are fine; the dialog fits the screen.
- **V17** D17 verified by L35: The ⋯ tap that scrolls the board no longer loses the menu: Move to… In progress completes from the Backlog view.
- **F22** fix for D18: Second pass after L36: the fact labels are flex: none + nowrap, so 'DUE DATE' stays one line and the value takes the remaining width.
- **S37** index.html · 375×812 · click #card-SB-108 · wait 200 — phone: dialog for an overdue card (D18 second pass) (screens not kept)
  - 👁 **L37**: Phone dialog for Search API pagination after the second pass. All three labels — ASSIGNEE, DUE DATE, PRIORITY — sit on one line each at the left of the facts panel, vertically centred on their values: "JW Jonas Weber", "Tue, Sep 22, 2026" with the red "Overdue by 1 day" chip right-aligned under it, and "Urgent" with the red square. The panel now reads as a three-row list; the rest of the dialog is unchanged and fits the screen.
- **V18** D18 verified by L37: Facts read as a clean label/value list on phones; no label wraps.
- **S38** index.html · 1280×800 · click #new-task · wait 300 — New task opens the Backlog form (screens not kept)
  - 👁 **L38**: Desktop, "New task" clicked. The page scrolls down to the Backlog's foot, where the inline form is open under Responsive images for case studies: "Card title" label, the focused textarea with its indigo ring and "What needs doing?" placeholder, then Add and Cancel. Right destination and focus — typing goes straight in. But the reveal stops with the form flush against the bottom of the screen: the white panel's lower edge touches the viewport edge, so the Add/Cancel row looks like it is about to be cut off. The header (and the button that was clicked) has scrolled out of view, which is acceptable for a jump to the form.
    - ✗ **D19** defect `.add-form (reveal)`: Opening a column's add form (New task, or a header +) scrolls it only until it touches the viewport's bottom edge, so the form's panel sits flush with the screen edge and reads as clipped.
- **S39** index.html · 1280×800 · focus #card-SB-102 · press Space · press ArrowRight · press ArrowRight · press Escape · wait 300 — keyboard: move cancelled with Escape (screens not kept)
  - 👁 **L39**: Desktop: Space on Footer links 404 on /press, → twice (it travelled to Review), then Escape. It is back exactly where it started — Backlog position 2, between Audit legacy URL redirects and Cookie banner copy review — flat, with only the keyboard focus ring; In progress is back to "WIP 3 / 3", Review to 2, and no column is tinted. The idle hint bar is showing again. The cancel restores the board precisely.
- **F23** fix for D19: The add form carries scroll-margin 16px 12px 32px, so the reveal after New task / + / Add a card leaves 32px of column below it instead of stopping at the edge.
- **S40** index.html · 1280×800 · click #new-task · wait 300 — New task opens the Backlog form (after D19) (screens not kept)
  - 👁 **L40**: Desktop, "New task" after D19. The page now scrolls to its end: the Backlog form is fully in view with its Add/Cancel row, the Backlog panel's rounded bottom edge and a strip of canvas below it, so nothing reads as clipped. The textarea is focused (indigo ring). The column headers have scrolled just off the top — the jump puts the thing you are about to type into in the middle of attention, which is the point of the button.
- **V19** D19 verified by L40: Form revealed with the column's bottom edge and canvas below it, not flush with the screen edge.
- **G32** `vlmkit check interactions index.html --max-elements 60` → exit 0 — elements 44 · suspects 0 · warns 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G32))
- **G33** `vlmkit check motion index.html` → exit 0 — activeAnimations 0 · running 0 · issues 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G33))
- **S41** index.html · 1280×800 · element .appbar · press Tab · wait 100 — first Tab: skip link (screens not kept)
  - 👁 **L41**: App bar close-up after the first Tab. "Skip to board" appears top-left as a dark pill with the indigo focus ring, laid over the brand — the "ard" of "Sprintboard" peeks out to its right. It is the conventional transient skip link (it disappears on the next Tab), readable and first in the order; the partial overlap of the wordmark is only visible while it has focus, so I leave it.
- **S42** index.html · 1280×800 · element .app-header · press Tab · press Tab · press Tab · press Tab · press ArrowRight · wait 200 — Tab to the chips, ArrowRight selects Design (screens not kept)
  - 👁 **L42**: Header close-up after four Tabs (skip link, search, New task, chip group) and ArrowRight. The radio group moved its selection to Design: the Design chip is dark with its purple dot ringed white and carries a clear 2px indigo focus ring offset from the pill, while All returns to outline. The summary switched to "Showing 5 of 14 cards · Clear filters". So the invisible radio inputs still give a visible keyboard focus on the chip itself.

### R4 · final check (builder)

- **G34** `vlmkit check integrity index.html` → exit 0 — verdict: CLEAN (0 fail, 0 warn, 294 exempted) ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G34))
- **G35** `vlmkit check copy index.html --manifest copy.txt` → exit 0 — missing 0 · placeholders 0 · manifestLines 9 · statesExplored 14 · revealedOnly 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G35))
- **G36** `vlmkit check design index.html` → exit 0 — verdict: COHERENT (0 finding(s)) ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G36))
- **G37** `vlmkit check composition index.html` → exit 0 — verdict: COMPOSED (0 finding(s)) ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G37))
- **G38** `vlmkit check color index.html` → exit 0 — verdict: CONSISTENT (0 finding(s)) ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G38))
- **G39** `vlmkit check a11y contrast index.html --output-dir /tmp/claude-0/-home-user-vlmkit/663fea0b-e0f8-5798-a548-7572e9f0176c/scratchpad/a11y-contrast` → exit 0 — inspected 183 · failures 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G39))
- **G40** `vlmkit check a11y contrast 'file:///home/user/vlmkit/examples/sites/kanban/index.html?replay=drop' --output-dir /tmp/claude-0/-home-user-vlmkit/663fea0b-e0f8-5798-a548-7572e9f0176c/scratchpad/a11y-contrast-2` → exit 0 — inspected 184 · failures 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G40))
- **G41** `vlmkit check a11y focus index.html --output-dir /tmp/claude-0/-home-user-vlmkit/663fea0b-e0f8-5798-a548-7572e9f0176c/scratchpad/a11y-focus` → exit 0 — steps 40 · findings 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G41))
- **G42** `vlmkit check a11y focus index.html --viewport 1280x1100 --output-dir /tmp/claude-0/-home-user-vlmkit/663fea0b-e0f8-5798-a548-7572e9f0176c/scratchpad/a11y-focus-tall` → exit 0 — steps 40 · findings 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G42))
- **G43** `vlmkit check a11y touch index.html --output-dir /tmp/claude-0/-home-user-vlmkit/663fea0b-e0f8-5798-a548-7572e9f0176c/scratchpad/a11y-touch` → exit 0 — level AA · inspected 39 · failures 0 · wcagExempt 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G43))
- **G44** `vlmkit check a11y touch index.html --viewport 375x812 --output-dir /tmp/claude-0/-home-user-vlmkit/663fea0b-e0f8-5798-a548-7572e9f0176c/scratchpad/a11y-touch-m` → exit 0 — level AA · inspected 39 · failures 0 · wcagExempt 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G44))
- **G45** `vlmkit scan scroll index.html` → exit 0 — containers 0 · overflowX 0 · issues 20 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G45))
- **G46** `vlmkit scan scroll index.html --viewport 375x812` → exit 0 — containers 1 · overflowX 0 · issues 20 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G46))
- **G47** `vlmkit scan handlers index.html --probe-drag` → exit 1 — registrations 175 · elements 69 · suspects 4 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G47))
- **G48** `vlmkit scan handlers 'file:///home/user/vlmkit/examples/sites/kanban/index.html?animate=0' --probe-drag` → exit 0 — registrations 175 · elements 69 · suspects 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G48))
- **G49** `vlmkit check interactions index.html --max-elements 60` → exit 0 — elements 44 · suspects 0 · warns 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G49))
- **G50** `vlmkit check motion index.html` → exit 0 — activeAnimations 0 · running 0 · issues 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G50))
- **G51** `vlmkit check animation 'file:///home/user/vlmkit/examples/sites/kanban/index.html?replay=drop'` → exit 0 — animations 2 · settleMs 180 · issues 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G51))
- **G52** `vlmkit check animation 'file:///home/user/vlmkit/examples/sites/kanban/index.html?replay=drop&animate=0'` → exit 0 — animations 0 · settleMs 0 · issues 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G52))
- **G53** `vlmkit check animation index.html` → exit 0 — animations 0 · settleMs 0 · issues 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G53))
- **G54** `vlmkit check scroll index.html --viewport 375x812` → exit 0 — pageScrolled 262 · stickyFixed 0 · engagedSticky 0 · snaps 1 · issues 1 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G54))
- **G55** `vlmkit check breakpoints index.html --sweep` → exit 0 — breakpoints 3 · issues 0 · sweepRanges 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G55))
- **G56** `vlmkit check grounding index.html` → exit 0 — targets 36 · scale 0.5 · issues 5 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G56))
- **G57** `vlmkit check grounding index.html --viewport 1280x1000` → exit 0 — targets 38 · scale 0.48 · issues 1 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G57))
- **G58** `vlmkit stress i18n index.html --output-dir /tmp/claude-0/-home-user-vlmkit/663fea0b-e0f8-5798-a548-7572e9f0176c/scratchpad/i18n` → exit 1 — inflate 1.4 · inspected 291 · overflowing 103 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G58))
  - ✗ **D20** defect `.column-head .count`: Under 1.4x text inflation (G58's after-image) the In progress header wraps its title to two lines and breaks the WIP pill inside itself — 'WIPXX 3X /X' over '3X' — so the count spills out of its pill. A longer translated column name ('In Bearbeitung') in a 250px column would do the same: the pill has no nowrap and the header gives the title no room to wrap on its own.
- **F24** fix for D20: Count pills and the + button are flex: none with white-space: nowrap, so 'WIP 3 / 3' never breaks; the h2 title gets min-width: 0 and overflow-wrap: break-word, so a long column name wraps inside the title instead of squeezing the pill.
- **G59** `vlmkit stress i18n index.html --output-dir /tmp/claude-0/-home-user-vlmkit/663fea0b-e0f8-5798-a548-7572e9f0176c/scratchpad/i18n-2` → exit 1 — inflate 1.4 · inspected 291 · overflowing 103 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G59))
- **F25** fix for D20: Second pass after G59's after-image: with break-word the unbreakable 'progressXXXX' kept its min-content width inside the flex h2 and ran under the pill; overflow-wrap: anywhere lets the title's text shrink with the h2 (words still wrap at spaces first, and only a word longer than the whole line is broken).
- **G60** `vlmkit stress i18n index.html --output-dir /tmp/claude-0/-home-user-vlmkit/663fea0b-e0f8-5798-a548-7572e9f0176c/scratchpad/i18n-3` → exit 1 — inflate 1.4 · inspected 291 · overflowing 103 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G60))
- **G61** `vlmkit stress i18n index.html --output-dir /tmp/claude-0/-home-user-vlmkit/663fea0b-e0f8-5798-a548-7572e9f0176c/scratchpad/i18n-4 --json` → exit 1 — inflate 1.4 · inspected 291 · overflowing 103 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G61))
  - ✗ **D21** defect `.avatar`: Avatars are fixed circles (24px on cards, 32px in the header), so any initials longer than two letters spill out: G61 measures 'MRX' at 26px inside the 24px circle on SB-105, SB-109 and SB-114, pushing each card's top row 2px past its box. Three-letter initials are ordinary (e.g. M. R. Díaz).
- **V20** D20 verified by G60: G60's after-image: the In progress header wraps its title to 'InX / progress / XXXX' inside the h2 while 'WIPXX 3X /X 3X' stays one line in its pill beside it; no overlap, + still at the right.
- **F26** fix for D21: Avatars are min-width circles with inline padding and border-radius 999px (header 32px, phone 28px, cards and dialog 24px): two initials still render as a circle, longer ones widen into a pill.
- **G62** `vlmkit stress i18n index.html --output-dir /tmp/claude-0/-home-user-vlmkit/663fea0b-e0f8-5798-a548-7572e9f0176c/scratchpad/i18n-5 --json` → exit 1 — inflate 1.4 · inspected 291 · overflowing 98 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G62))
- **V21** D21 verified by G62: G62: the six visible horizontal-overflow rows (3 avatars + the 3 top rows holding them) are gone; its after-image shows 'MRX', 'JWX', 'PNX' as pills that fit their text, in the header and on every card.
- **N21** false-positive about G62: What remains in G62 is not an overflow a reader can see. 65 horizontal-overflow rows are the page's .sr-only spans (scrollW > clientW 1 is how a 1px visually-hidden box looks — the same elements check integrity exempts). The 33 vertical-wrap rows are warns that text wrapped to more lines under 1.4x inflation (card titles to two lines, meta rows to a second line, the In progress title to three) — the gate's own report calls these 'usually fine'; its after-image shows every card and header growing to fit with nothing clipped or overlapping.
- **N22** superseded about G58: Earlier stress i18n run in the D20/D21 loop; superseded by G62.
- **N23** superseded about G59: Earlier stress i18n run in the D20/D21 loop; superseded by G62.
- **N24** superseded about G60: Earlier stress i18n run in the D20/D21 loop; superseded by G62.
- **N25** superseded about G61: Earlier stress i18n run in the D20/D21 loop; superseded by G62.
- **N26** decision: Added stress i18n to the final gate set: the brief asks for 'a board that still reads when it is full', and a kanban's density problem is text length — long titles, translated column names, long labels. At 1.4x inflation it found two real layout faults no screenshot of the English page could show (D20 the WIP pill breaking and the title running under it; D21 fixed-width avatar circles), both fixed in this round. Because this round changed layout CSS after the first final-round gate runs (G34–G57), the whole gate set is run once more after the last change; the notes attach to those last runs.
- **S43** index.html · 1280×800 · full page, 2 screen(s) — final: full board ([screens](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#S43))
  - 👁 **L43**: Final desktop 1280, two screens (942px). What reads first is right: "Sprintboard" and its indigo mark, the indigo New task button, then "Website relaunch" as the page's largest text with "Sprint 14 · Sep 14 – 25 · 14 cards" under it and the five readable team circles (AK MR PN JW LO) at the right. Chips All (dark) / Design / Frontend / Backend / Bug with colour dots, "Showing all 14 cards" flush right. Four tinted columns with clear borders, headers "Backlog 5", "In progress WIP 3 / 3", "Review 2", "Done 4", each with its +. Fourteen white cards share one anatomy — labels + avatar + ⋯, bold title, one meta line — and the priority bars, checklist counts, dates, the two red "Overdue" pills and the amber "Due today" pill are all legible. Short columns stretch to the Backlog's height and end with "Add a card". No crowding, clipping or ragged rows; avatars are still circles after D21.
- **S44** index.html · 768×1024 · full page, 1 screen(s) — final: full board ([screens](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#S44))
  - 👁 **L44**: Final tablet 768x1024, one screen. Header as on desktop with a 220px search; team circles readable. The board is the sideways row at 288px columns: Backlog and In progress whole, Review cut at the right edge ("Navigation key…", "Broken logo o…"), which signals that it scrolls. Cards identical to desktop; every meta row fits one line at this width. The board ends at y≈900 and the canvas below is empty, as expected for 14 cards.
- **S45** index.html · 375×812 · full page, 2 screen(s) — final: full board ([screens](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#S45))
  - 👁 **L45**: Final phone 375x812, two screens (1074px). Two-row app bar (brand; search + New task), the title and caption, five readable 28px circles, chips on two lines, the segmented switcher with "Backlog" current, then the Backlog column (first card at y≈402) with the In progress column peeking 16px at the right. Screen 2 shows the rest of the Backlog down to "Add a card". Cards keep the desktop anatomy and fit their meta on one line at 315px. No horizontal page overflow; the only sideways movement is the board's own snapped row.
- **S46** index.html · 1280×800 · focus #card-SB-106 · press Space · press ArrowRight · press ArrowDown · wait 250 — STATE: card picked up with the keyboard, mid-move (desktop) (screens not kept)
  - 👁 **L46**: STATE, desktop: card picked up with the keyboard mid-move (Space, →, ↓ on Hero copy). Hero copy hangs in Review at position 2, tilted with a deep shadow and the indigo outline; Review is tinted as the target and counts 3, In progress reads "WIP 2 / 3". The one-line hint bar says "Moving ‘Hero copy’ · Review, position 2 of 3" with the four key groups. The carried card and both its neighbours are fully in view; the bar only overlaps the Backlog's last card and Done's "Add a card" while the move lasts.
- **S47** index.html · 375×812 · focus #card-SB-106 · press Space · press ArrowRight · press ArrowDown · wait 500 — STATE: card picked up with the keyboard, mid-move (phone) (screens not kept)
  - 👁 **L47**: STATE, phone: the same move. The board has scrolled and snapped to Review (switcher pill on "Review"); Hero copy is lifted at position 2 between Navigation keyboard support and Broken logo on older Safari, tinted column, count 3. The hint block at the bottom: "Moving ‘Hero copy’ · Review, position 2 of 3" on one line and the keys in a 2×2 grid; it covers only the lower part of the third card, whose title stays readable.
- **S48** index.html · 1280×800 · click li[data-id='SB-109'] .card-menu-btn · click .menu-item[data-to='progress'] · wait 400 — STATE: In progress limit exceeded (desktop) (screens not kept)
  - 👁 **L48**: STATE, desktop: In progress over its limit after ⋯ → Move to… In progress on Navigation keyboard support. Red-tinted column with red border, red "WIP 4 / 3" pill, and under the header the alert "Over the WIP limit by 1 card" (bold red) / "Finish something before starting more." (grey, two balanced lines). The moved card is fourth and keeps its "Due today" chip; Review drops to 1. The over-limit state is explicit in text and colour both.
- **S49** index.html · 375×812 · click li[data-id='SB-109'] .card-menu-btn · click .menu-item[data-to='progress'] · wait 500 — STATE: In progress limit exceeded (phone) (screens not kept)
  - 👁 **L49**: STATE, phone: the same over-limit move. The board followed the card to In progress (switcher pill "In progress"); the column is red-tinted with "WIP 4 / 3" and the alert reads "Over the WIP limit by 1 card" / "Finish something before starting more." on one line each at this width. Four cards, Navigation keyboard support last; nothing clipped.
- **S50** index.html · 1280×800 · focus #card-SB-102 · press Enter · wait 250 — STATE: card detail dialog open (desktop, opened with Enter) (screens not kept)
  - 👁 **L50**: STATE, desktop: card detail dialog opened from the keyboard (focus Footer links 404 on /press, Enter). The dialog is centred over the dimmed board and focus landed on its × close, which now shows the indigo keyboard ring. Kicker "● Backlog · SB-102", title, red Bug and blue Frontend chips, the facts panel (PN Priya Nair; Mon, Sep 21, 2026 with a red "Overdue by 2 days" pill; High), the description, and CHECKLIST "1 of 3 done" with the bar at a third and one struck-through item. Everything the brief lists for the detail is there and legible.
- **S51** index.html · 375×812 · click #card-SB-106 · wait 250 — STATE: card detail dialog open (phone) (screens not kept)
  - 👁 **L51**: STATE, phone: the dialog for Hero copy. Full-width sheet with 16px margins, facts as a three-row list (Ana Kowalski / Fri, Sep 25, 2026 / High, all on one line each), description in four short lines, and the five checklist rows with 18px boxes and roomy rows for thumbs. It ends at y≈720 so it needs no scrolling; the board is dimmed behind it.
- **S52** index.html · 1280×800 · click input[name='label-filter'][value='frontend'] · wait 250 — STATE: filter applied — Frontend (desktop) (screens not kept)
  - 👁 **L52**: STATE, desktop: filter applied — Frontend. The Frontend chip is the dark one; "Showing 5 of 14 cards · Clear filters". Backlog shows its two Frontend cards and "3 cards hidden by the filter", In progress one card and "2 cards hidden by the filter", Review both of its cards (both Frontend, so no note), Done the dashed "No cards match the filter". Counts stay the column totals, and every column explains the difference. The board shrinks to content; nothing shifts sideways.
- **S53** index.html · 375×812 · full page, 1 screen(s) · click input[name='label-filter'][value='bug'] · wait 250 — STATE: filter applied — Bug (phone) (screens not kept)
  - 👁 **L53**: STATE, phone: filter applied — Bug, full page (one screen). Bug chip dark with its red dot; "Showing 3 of 14 cards · Clear filters" appears under the chips; the Backlog column shows Footer links 404 on /press (Bug · Frontend, Overdue) and "4 cards hidden by the filter", and the peeking In progress column shows the dashed empty box. The switcher still points at Backlog.
- **S54** index.html · 1280×800 · click #col-review .add-card-btn · wait 250 — STATE: add-card form open (desktop, Review) (screens not kept)
  - 👁 **L54**: STATE, desktop: the add-card form open in Review (its "Add a card" clicked). The button is replaced by a card-like white form under Broken logo on older Safari: bold "Card title", an empty two-line field with the indigo focus ring and the "What needs doing?" placeholder, indigo Add and ghost Cancel. Title, Add, Cancel — exactly the three parts the brief names — and the rest of the board is untouched.
- **S55** index.html · 375×812 · scroll-to #col-review · wait 600 — STATE: phone snapped to the third column (screens not kept)
  - 👁 **L55**: STATE, phone snapped to the third column. After scrolling the board to Review the row settled with Review's left edge on the 16px gutter, a sliver of In progress at the left and Done peeking at the right; the switcher's white pill is on "Review". Review shows its "2" count, Navigation keyboard support (Due today), Broken logo on older Safari and "Add a card". One column fills the screen, as the brief asks for phones.
- **S56** index.html · 375×812 · full page, 2 screen(s) · click [data-jump='2'] · wait 700 — STATE: phone, switcher 'Review' tapped (screens not kept)
  - 👁 **L56**: Phone, full page after tapping "Review" in the switcher. The board jumped to the Review column and snapped the same way as S55 (left edge on the gutter, neighbours peeking), and the pill moved to "Review" — the switcher is a working shortcut, not a decoration. Screen 2 shows the rest of the Review panel running empty to the board's end, since every column takes the Backlog's height.
- **G63** `vlmkit check integrity index.html` → exit 0 — verdict: CLEAN (0 fail, 0 warn, 294 exempted) ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G63))
- **G64** `vlmkit check copy index.html --manifest copy.txt` → exit 0 — missing 0 · placeholders 0 · manifestLines 9 · statesExplored 14 · revealedOnly 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G64))
- **G65** `vlmkit check design index.html` → exit 0 — verdict: COHERENT (0 finding(s)) ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G65))
- **G66** `vlmkit check composition index.html` → exit 0 — verdict: COMPOSED (0 finding(s)) ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G66))
- **G67** `vlmkit check color index.html` → exit 0 — verdict: CONSISTENT (0 finding(s)) ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G67))
- **G68** `vlmkit check a11y contrast index.html --output-dir /tmp/claude-0/-home-user-vlmkit/663fea0b-e0f8-5798-a548-7572e9f0176c/scratchpad/f-contrast` → exit 0 — inspected 183 · failures 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G68))
- **G69** `vlmkit check a11y focus index.html --output-dir /tmp/claude-0/-home-user-vlmkit/663fea0b-e0f8-5798-a548-7572e9f0176c/scratchpad/f-focus` → exit 0 — steps 40 · findings 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G69))
- **G70** `vlmkit check a11y touch index.html --output-dir /tmp/claude-0/-home-user-vlmkit/663fea0b-e0f8-5798-a548-7572e9f0176c/scratchpad/f-touch` → exit 0 — level AA · inspected 39 · failures 0 · wcagExempt 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G70))
- **G71** `vlmkit check a11y touch index.html --viewport 375x812 --output-dir /tmp/claude-0/-home-user-vlmkit/663fea0b-e0f8-5798-a548-7572e9f0176c/scratchpad/f-touch-m` → exit 0 — level AA · inspected 39 · failures 0 · wcagExempt 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G71))
- **G72** `vlmkit scan scroll index.html` → exit 0 — containers 0 · overflowX 0 · issues 20 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G72))
- **G73** `vlmkit scan scroll index.html --viewport 375x812` → exit 0 — containers 1 · overflowX 0 · issues 20 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G73))
- **G74** `vlmkit scan handlers index.html --probe-drag` → exit 1 — registrations 175 · elements 69 · suspects 4 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G74))
- **G75** `vlmkit scan handlers 'file:///home/user/vlmkit/examples/sites/kanban/index.html?animate=0' --probe-drag` → exit 0 — registrations 175 · elements 69 · suspects 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G75))
- **G76** `vlmkit check interactions index.html --max-elements 60` → exit 0 — elements 44 · suspects 0 · warns 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G76))
- **G77** `vlmkit check motion index.html` → exit 0 — activeAnimations 0 · running 0 · issues 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G77))
- **G78** `vlmkit check animation 'file:///home/user/vlmkit/examples/sites/kanban/index.html?replay=drop'` → exit 0 — animations 2 · settleMs 180 · issues 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G78))
- **G79** `vlmkit check animation 'file:///home/user/vlmkit/examples/sites/kanban/index.html?replay=drop&animate=0'` → exit 0 — animations 0 · settleMs 0 · issues 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G79))
- **G80** `vlmkit check animation index.html` → exit 0 — animations 0 · settleMs 0 · issues 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G80))
- **G81** `vlmkit check scroll index.html --viewport 375x812` → exit 0 — pageScrolled 262 · stickyFixed 0 · engagedSticky 0 · snaps 1 · issues 1 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G81))
- **G82** `vlmkit check breakpoints index.html --sweep` → exit 0 — breakpoints 3 · issues 0 · sweepRanges 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G82))
- **G83** `vlmkit check grounding index.html` → exit 0 — targets 36 · scale 0.5 · issues 5 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G83))
- **G84** `vlmkit check grounding index.html --viewport 1280x1000` → exit 0 — targets 38 · scale 0.48 · issues 1 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G84))
- **N27** false-positive about G74: Final-pass drag probe: the only suspects are the 4 drag-cancel-not-reverted rows, which come from the probe baselining its Escape gesture inside the 180ms settle of the real drop it made just before. The control run G75 — identical probe on ?animate=0, which removes only that settle — has 0 suspects, so a cancelled drag leaves the card exactly as it was (see N13/N14/N15). drag-ghost-illegible no longer appears (the titled placeholder, D8).
- **N28** accepted about G74: unprobed-handler-types (warn) is the gate's standing disclaimer that it presses nothing; G76 (check interactions) exercises the click/keydown/focus/change/close paths and the state shots S46–S56 exercise the rest.
- **N29** accepted about G75: Control run for G74 (?animate=0): 0 suspects. Its only line is the same unprobed-handler-types disclaimer, covered by G76 and the state shots.
- **N30** false-positive about G72: All 20 clipped-content warns are .sr-only spans (screen-reader text for priority, checklist, due, assignee, and the chips' legend), the pattern check integrity G63 exempts; page overflow-x is 0 and no scroll container is declared at 1280, which is correct for the 4-up grid. The 5 dead scrollports are the four hidden textareas (UA overflow:auto) and the closed dialog's overflow guard. Same analysis as N5.
- **N31** false-positive about G73: Same 20 .sr-only clipped-content warns at 375x812 (see G72/N5). The substantive result is right: page overflow-x 0 and one scroll container, #board axis=x overflow=953px — the snapped phone row.
- **N32** false-positive about G81: snap-not-snapping again ignores the board's scroll-padding-inline: 16px — the first column's snap position is 16 − 16 = 0, exactly where it 'settled'. S55/L55 and S56/L56 (final round) show Review landing on the 16px gutter both after a scroll and after the switcher tap. Same as N8/N9.
- **N33** accepted about G83: Frame-edge warns: SB-105 and Done's 'Add a card' are cut to 1px by the bottom of the 720px frame, so their click points sit in that sliver — hence imprecise-target and crowded-target (5px from the card above). G84 measures the same page at 1280x1000 with the whole board in frame: both imprecise and both crowded warns disappear. The remaining ambiguous-target ('Add a card' ×3) is accepted as in N10: each sits under its own column header, the accessible names are unique via aria-label, and the text is required copy.
- **N34** accepted about G84: With the whole board in frame, the one remaining warn is ambiguous-target for the four 'Add a card' buttons — accepted for the reasons in N10 (column context on screen, unique aria-label names, required verbatim copy); the tool note N11 covers the gate's advice asking for a name the buttons already have.
- **done**: Sprintboard's 'Website relaunch' board: a static, deterministic kanban (14 seeded cards, fixed 'today' of Sep 23 2026) with HTML5 drag and drop, a full keyboard path (Space lift, arrows, Space drop, Escape cancel, live-region announcements and a visible hint bar), a ⋯ menu with Move to…, a modal card dialog whose checklist writes through to the card, inline add forms, label filters and search, a WIP limit that turns the column red and says so, and a snapped one-column row on phones with a switcher. Four rounds, 21 defects: 15 found by eye in the shots (avatar overlap, avatar-induced meta wraps, dead priority bars, the phone header and New task row, the squeezed and orphaning hint bar, a carried card jammed at the screen edge, an alert without hierarchy, notes blaming 'the filter' for a search, the phone menu dismissed by its own scroll, ragged phone dialog facts, the add form revealed flush) and 6 by gates (meta near-misalignment, two off-scale paddings, an illegible drag ghost, the unmeasured settle, the WIP pill breaking under longer text, fixed-width avatars) plus one gate suspicion (cancel not reverted) that turned out to be the probe's own timing. Final gate set clean except documented false positives: sr-only spans read as clipped content (scan scroll, stress i18n), snap judged without scroll-padding (check scroll), the drag probe baselining inside its own drop's settle, and the frame-edge grounding warns; 'Add a card' ×4 ambiguity accepted.
- **done**: Correction to the summary above, whose defect counts were miscounted: 21 defects — 13 found by eye in the shots (D1 avatar overlap, D2 avatar-induced meta wraps, D3 dead priority bars, D4 New task pushed to its own phone row, D5 480px phone header, D12 squeezed hint bar, D13 carried card jammed at the screen edge, D14 over-limit alert without hierarchy, D15 notes blaming 'the filter' for a search, D16 phone hint orphaning its count, D17 phone menu dismissed by its own scroll, D18 ragged phone dialog facts, D19 add form revealed flush) and 8 found by gates (D6 meta near-misalignment, D7 off-scale paddings, D8 illegible drag ghost, D9 cancel-not-reverted — which proved to be the probe's own timing, D10 cards not announcing their dialog, D11 unmeasured settle, D20 WIP pill breaking under longer text, D21 fixed-width avatars). Final gate set clean except documented false positives (sr-only spans read as clipped content by scan scroll and stress i18n; snap judged without scroll-padding by check scroll; the drag probe baselining inside its own drop's settle; frame-edge grounding warns) and the accepted 'Add a card' ×4 ambiguity.

### R5 · independent review (reviewer)

- **G85** `vlmkit check integrity 'index.html?animate=0'` → exit 1 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G85))
- **G86** `vlmkit check color index.html` → exit 0 — verdict: CONSISTENT (0 finding(s)) ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G86))
- **G87** `vlmkit check a11y contrast index.html` → exit 0 — inspected 183 · failures 0 ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G87))
- **G88** `vlmkit check design index.html` → exit 0 — verdict: COHERENT (0 finding(s)) ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G88))
- **N35** superseded about G85: The reviewer hit the same defect three builders reported: a relative path with a query string is read as a filename ('error: file not found: examples/sites/kanban/index.html?animate=0'). Fixed in vlmkit's source (sourceToUrl keeps a local page's ?query / #fragment) in this change, but dist/ is rebuilt only after the last agent finishes, so G89 re-runs it with an absolute file:// URL.
- **G89** `vlmkit check integrity 'file:///home/user/vlmkit/examples/sites/kanban/index.html?animate=0'` → exit 0 — verdict: CLEAN (0 fail, 0 warn, 294 exempted) ([output](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#G89))
- **S57** index.html?animate=0 · 1280×800 · full page, 2 screen(s) — the board as a team member opens it ([screens](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#S57))
  - 👁 **L57**: Desktop (2 screens). A light grey canvas under a white header (the Sprintboard mark, a search field, a violet "New task"), then the board title with its sprint caption and card count, five avatar initials on the right, and filter chips with coloured dots (All selected, dark) beside "Showing all 14 cards". Four columns on pale panels, each header with a status dot, name, count badge and +; In progress shows "WIP 3 / 3". Cards share one construction: label chips in colour AND text, an avatar, the title, then priority bars with a word, checklist progress with an icon, a due date — and "Overdue" (red) or "Due today" (amber) as worded chips. Screen 2 shows every column ending in an "Add a card" row; nothing is cut off and the columns share one top edge.
- **S58** index.html?animate=0 · 375×812 · full page, 2 screen(s) — the board as a team member opens it ([screens](https://mizchi.github.io/vlmkit/sites/kanban/judgment/#S58))
  - 👁 **L58**: Phone (2 screens). The header wraps to the brand and then search with New task; the title, avatars and the filter chips (on two rows) come next, then a segmented switcher — Backlog / In progress / Review / Done — above a row of snapped columns: Backlog fills the screen and the next column peeks in at the right edge, which says the row scrolls sideways. Cards keep every field at this width; the long title "Responsive images for case studies" wraps to two lines without pushing anything out.
- **S59** index.html?animate=0 · 1280×800 · focus #card-SB-101 · press Enter · wait 300 — a card's detail dialog, opened with Enter (screens not kept)
  - 👁 **L59**: Desktop, Enter on card SB-101: a modal dialog over a dimmed board — column and id as a breadcrumb, the title, the Backend label, an info strip (assignee with avatar, "Fri, Oct 2, 2026", priority), the description, and a checklist with a progress bar reading "1 of 4 done", the done item struck through. Focus is on the close button, drawn with a clear violet ring. Balanced, readable, nothing clipped.
- **N36** decision: Independent review: integrity (after the superseded G85), color, contrast and design re-run green; desktop, phone and the card dialog show nothing the builder's log missed. No defects raised.
- **done**: Reviewer round: gates green on re-run; board, phone layout and the card dialog hold up.

_The 68 screens were taken as JPEG and re-encoded as WebP (quality 0.75) after the round: 4.5 MB → 2.3 MB._
